# 캠퍼스 캐릭터 색 커스텀 — plan

작성: 2026-08-11
상태: **완료** — ①안(눈으로 확인)으로 승인·구현·배포. 대머리는 뒤에 기하 방식으로 다시 잡았다(2026-08-11)
근거: `research-campus-character-color.md`

## 0. 한 줄 요약
칸을 그냥 칠하면 머리색과 신발색이 같이 바뀐다(칸은 색 단위지 부위가 아니다).
부위를 **빈 칸으로 이사시킨 뒤** 칠한다. 막힌 곳은 기술이 아니라 **부위 분류 검증**이다.

## 1. 만들 것

**A. 매핑 표** — `public/campus/models/kenney/part-cells.json`
```json
{ "male-b": { "skin": [[7,6],[7,7]], "hair": [[5,7]], "top": [[4,4],[4,5]],
              "bottom": [[5,4]], "shoes": [[5,5]] } }
```
- 오프라인 스크립트가 뽑고(`scripts/campus-part-cells.py`), 사람이 확인한 뒤 커밋한다.
- 런타임 휴리스틱으로 두지 않는다 — 매번 다시 틀리고, 틀린 걸 고쳐 둘 자리가 없다.

**B. 색 바꾸기 = UV 이사** — `avatar-kenney.js`

팔레트를 열어 보니 **이미 16가지 색 계열이 들어 있다**(8열 × 2줄, y4~y7).
캐릭터 하나가 쓰는 건 6~10칸뿐이다. 그래서 **다시 칠할 필요가 없다** —
그 부위의 UV 를 다른 색 칸으로 옮기기만 하면 된다.

```
y0~y1  비어 있음(30칸)                          ← 2단계에서 자유 색을 칠할 자리
y2~y3  0열만 마젠타(누락 표식)
y4~y5  크림 초록 노랑 주황 다홍 파랑 연하늘 보라        ← 옷 색 8
y6~y7  먹색 회색 청회색 연회청 흰색 살구 갈색살 밝은살    ← 중립·피부 8
```

- `recolor(look.colors)` : 부위 정점의 UV 를 목표 색 칸으로 평행이동. 끝.
- **texture·material 복제가 필요 없다** — 12종이 계속 한 장을 공유한다.
  사람마다 복제하는 건 **geometry(UV) 하나뿐**이다(research §3 의 clone 함정은 여전히 유효).
- 명암 램프는 옮겨 간 칸에 이미 들어 있으므로 음영이 그대로 산다.
- 16색 밖의 색은 **2단계**로 미룬다(빈 칸에 램프를 복사해 색조 이동). 지금은 안 만든다.

**C. UI** — 캐릭터 패널 아래 색 줄
- 부위 4개(피부·머리·상의·하의) × 팔레트에 든 색. 자유 색상환은 안 쓴다
  (마을이 형광색으로 뒤덮인다). 고르면 즉시 반영 → `saveCharacter` → `net.updateMeta`
- 피부는 살색 계열 4개(살구·갈색살·밝은살·크림)만 보여 준다 — 초록 피부는 실수로만 눌린다

## 2. 결정할 것 — 부위 분류를 어떻게 확정할까

자동 분류가 12종 중 서넛 틀린다(research §4). 그림을 안 보고는 못 고친다.

| 안 | 방법 | 비용 | 결과 |
|---|---|---|---|
| **A (권장)** | 캠퍼스에 임시 디버그 모드를 넣어 부위를 형광색으로 칠하고 12종을 눈으로 확인한 뒤 표를 손보고 커밋 | 반나절 | 표가 확정된다. 이후 런타임은 표만 읽으면 된다 |
| B | 자동 분류를 믿고 진행 | 즉시 | 12종 중 서넛에서 "머리색을 눌렀는데 신발이 바뀐다" |
| C | 부위 이름을 포기. "이 캐릭터가 쓰는 색 N개"를 스와치로 나열해 각각 교체 | 적음 | 분류가 아예 필요 없다. 대신 무엇을 바꾸는지 눌러 봐야 안다 |

권장은 **A**. 한 번 확인해 두면 표는 에셋이 바뀌지 않는 한 다시 안 건드린다.
C 는 분류 문제를 우회하지만, 아이가 "머리 색"을 찾지 못하면 기능이 없는 것과 같다.

## 3. 하지 않는 것
- 자유 색상환 · 파츠 추가(모자·안경) · 얼굴 편집 — 표정·헤어는 메시에 그려져 있다
- 게스트 색 저장(캐릭터 패널이 이미 로그인 전용)
- 프로필로 내보내기 — `plan-profile-character.md` 쪽 별건

## 4. 단계
1. [완료] `scripts/campus-part-cells.py` + `part-cells.json`
2. [완료] 눈으로 확인 — 브라우저 디버그 모드 대신 **GLB 를 파이썬으로 소프트웨어
   렌더링**해 부위별 형광색 대조표를 뽑았다(라이브에 디버그 코드를 올리지 않아도 됐다).
   12종 중 10종이 자동으로 맞았고, `female-f`(머리가 실제로 두 계열) ·
   `male-e`(긴소매라 맨손이 없어 머리가 살로 잡힘) 둘만 `OVERRIDE` 로 덮었다
3. [완료] `recolor()` + geometry 복제. 실제 텍스처를 샘플링해 전/후 렌더로 확인
4. [완료] UI(부위별 스와치) + `saveCharacter` + `net.updateMeta`
5. [완료] 배포. 라이브에서 `part-cells.json` 200, 머리 정점 153개만 이동,
   두 인스턴스의 geometry 가 분리됨을 확인

**남은 것**: 소품(안경·마스크·휠체어 등 `aid-*`, `wheelchair-*`) — 같은 팩·같은 리그라
붙이기는 쉽다. 색 커스텀과 독립된 작업이다.

**로그인 계정으로 확인이 필요한 것**: 캐릭터 패널은 로그인 전용이라(`dressBtn.hidden = !ME`)
헤드리스로는 스와치를 눌러 볼 수 없었다. 색 적용·저장·재접속 유지는 코드 경로로만 확인했다.
