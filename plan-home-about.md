# 계획: `/` = 피드 · `/about` 소개 전면개편

작성: 2026-08-06 · 상태: **구현 완료 [완료] · 배포 대기**
미확정 2건(§6)은 기본값으로 진행: 경로는 `/about`, 피드 상단 소개 링크는 넣음(로그아웃 방문자에게만).
근거 조사: `research-home-about.md`

## 사용자 확정 사항
1. 루트(`/`)는 커뮤니티 피드, 소개는 `/about` 신설
2. 로그인 사용자 자동 이동 유지 (학생→`/learn`, 학부모→`/account`)
3. 소개 메시지 전략은 내가 세운다 → §2
4. 새 전략과 **충돌하는 것만 삭제**, 충돌 없는 내용은 유지하되 **디자인은 새로**

---

## 1. 라우팅 변경

| # | 작업 | 검증 |
|---|---|---|
| 1-1 | `src/app/page.tsx` → `<LoginRedirect /> + <CommunityShell />`. metadata: title "학습 피드 — Plantor" | `out/index.html`에 피드 셸 프리렌더 |
| 1-2 | `src/app/about/page.tsx` 신설 — 새 소개 페이지. metadata: 기존 랜딩 문구 + `alternates.canonical: "/about"` + OG | `out/about/index.html` 생성 |
| 1-3 | `src/app/community/page.tsx` 유지 (같은 셸). 기존 링크·북마크 보호. `alternates.canonical: "/"` 로 중복 색인 차단 | 빌드 통과 |
| 1-4 | `navbar.tsx` — "피드" 링크 `/community` → `/`, **"소개" 링크 `/about` 추가** (데스크톱 `:181` · 모바일 `:272`). 로고 `/`는 그대로 | 3경로 이동 확인 |
| 1-5 | `sitemap.ts` — `/about` priority 1.0 추가, `/` 0.9(daily)로 조정 | `out/sitemap.xml` |
| 1-6 | `layout.tsx` metadata — 루트 canonical 유지, description은 브랜드 기본값으로. 소개 문구는 `/about`이 소유 | 빌드 통과 |

`robots.ts`는 변경 없음(`/community`·`/about` 모두 허용 상태).

**주의**: `LoginRedirect`는 루트에만 둔다. `/about`·`/community`에 붙이면 학부모가 소개를 못 읽는다.

---

## 2. 소개 페이지 전략 (내 제안)

### 축 이동: "학원 프로그램이 싸다" → **"싸게 시작해서, 아이가 계속 하게 만든다"**

**왜 바꾸나**
- 홈러닝이 깨지는 지점은 가격이 아니라 **지속**이다. "월 1.5만원"은 결제를 부르지만 "그래서 우리 애가 하겠냐"는 진짜 불안은 못 막는다.
- 최근 1년 업데이트(자동 수집 → XP·레벨·뱃지·상점·피드)가 전부 그 불안 한 점에 꽂혀 있다. 프로그램 중개는 누구나 하지만, **결과를 자동으로 긁어와 리워드로 되돌려주는 층**은 플랜토만 가진 자산이다.
- 가격은 버리지 않는다. 진입 문턱 제거용으로 히어로 서브라인과 라인업 섹션에 그대로 남긴다.

### 섹션 구성 (`/about`)

| # | 섹션 | 내용 | 출처 |
|---|---|---|---|
| 1 | **Hero** | 헤드라인 "집에서 하는 공부가, 오늘도 이어지게" / 서브 "학원이 쓰는 프로그램을 월 1.5만원부터. 학습 결과는 플랜토가 알아서 가져와 기록합니다." / CTA `신청하기` + `오늘의 피드 보기(/)` | 신규 |
| 2 | **살아있는 증거** | 실제 피드 요약을 그대로 노출 — 오늘 n명 공부 · 모은 XP · 최고 연속 · 지금 학습 중 + 최근 카드 3장 | `FeedList`의 `TodaySummary` 로직을 `AboutFeedPeek`로 분리 재사용 |
| 3 | **어떻게 굴러가나** | ① 검증된 프로그램에서 공부한다 ② 플랜토가 결과를 자동으로 가져온다(직접 입력 0) ③ XP·레벨·뱃지로 돌아온다. 자동 수집 4종(매일국어·오토보카·클래스카드·클래스5) 아이콘 | 스크래퍼 4종 |
| 4 | **리워드 설계** | 레벨 9단계(씨앗→큰나무) · 뱃지 45종/희귀도 4단계·대부분 히든 · 상점(캐릭터 26종+CSS 치장) · 연속 학습 배수 최대 ×1.5. **"랭킹이 아니라 시간순 피드"** 원칙 명시 | `catalog.ts`, `rewards-config.ts` |
| 5 | **채점이 후하지 않다** | 품질 점수는 실측 분포 기준. 매일국어는 추천 독해속도 2배 초과 시 품질 절반 — 빨리 넘기기로는 레벨이 안 오른다 | `QUALITY_ANCHOR`, `SPEED_PENALTY_*` |
| 6 | **학부모가 보는 것** | 등원시간 0분 · 학습 현황 자동 기록 · 재구독률 93% | 기존 `CORE_VALUES` 2·3·4 (내용 유지) |
| 7 | **서비스 라인업** | `ServicesSection` **그대로 재사용** (어드민 인라인 편집·Firestore 오버라이드 보존) | 기존 |
| 8 | **신청 CTA + FAQ** | 내용 유지, 디자인 신규 | 기존 |

§5는 신규 섹션이다. "게임 요소 붙였다"는 흔한 말에 **"대충 하면 안 오른다"**를 붙여야 학부모에게 신뢰가 생긴다 — 이 근거가 실제 코드에 있는데 소개에 없다.

---

## 3. 충돌 정리 (삭제/수정)

| 대상 | 처리 | 근거 |
|---|---|---|
| `CORE_VALUES[0]` "학부모에게 교사 계정을 드립니다 — 진도 설정·과제 부여·관리" | **삭제**, "직접 입력할 게 없습니다 — 결과를 플랜토가 가져옵니다"로 교체 | 교사 계정은 운영자가 스크래핑에 쓰는 자격(`verify-auto.ts:15`). 학부모에게 발급 안 함 → **사실과 다른 약속** |
| FAQ "아이가 실제로 학습하고 있는지 확인할 수 있나요?" 답 | **갱신** — 자동 수집·XP·피드 기준으로 | 옛 설명이 현재 기능보다 약함 |
| `hero.tsx` 카피 | 교체 (§2) | 축 이동 |
| `values.tsx` · `cta.tsx` · `signup-cta-banner.tsx` · `faq.tsx` | 내용 유지, **디자인 신규** | 사용자 지시 4 |
| `services-section.tsx` | **손대지 않음** | 어드민 편집 회귀 위험 |
| `STATS` (`data/site.ts`) | **손대지 않음, 보고만** | 이번 작업 이전부터 있던 미사용 데이터 — 삭제는 별건 |

## 4. 디자인 방침

- 새 섹션은 **Tailwind v4 + `--color-p-*` 토큰**으로 작성해 앱 나머지와 통일한다. 랜딩만 쓰던 인라인 `style` + `T` 토큰 방식은 `services-section`에만 남긴다.
- 기존 랜딩의 "흰 배경 · 회색 구분선 · 사각 버튼(radius 4)"을 유지하되, 피드 카드와 같은 라운드(12px)·그림자 체계로 맞춰 **소개와 실제 제품 화면이 같은 물건으로 보이게** 한다.
- 모바일 우선. 전역 `overscroll-behavior:none` 규칙 유지.

## 5. 검증

1. [완료] `npm run build` → `/` · `/about` · `/community` 프리렌더 확인
2. [완료] 손댄 파일 lint 에러 0
3. [완료] `out/sitemap.xml` — `/about` priority 1.0, `/` 0.9 daily
4. [완료] 네비 링크(피드 `/` · 소개 `/about`) 반영
5. [완료] 배포 후 라이브 확인 — `/` 피드(카드 40장·소개 링크 노출), `/about` 10개 섹션 전부 렌더,
   `/community` canonical `/`, 모바일 375px 가로 오버플로 0

## 6. 미확정 — 승인 시 함께 답 주면 좋을 것

- **`/about` 경로명**: `/about` vs `/intro` vs `/service`. 기본값 `/about`으로 진행.
- **피드에 로그아웃 방문자용 진입 안내**: 루트가 피드라 첫 방문자가 맥락 없이 카드만 볼 수 있음. 피드 상단에 "플랜토가 뭔가요? →" 한 줄 링크를 넣을지. 기본값 **넣는다**.
