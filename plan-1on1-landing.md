# Plan: `/on` 직강 홍보 페이지 설계

> 운영자(본인)가 **직접 수업하는 1:1 직강**을 홍보하는 랜딩 페이지.
> 앱 기능 화면이 아니라 **마케팅 페이지** (랜딩 `/`, `/momsaipack` 계열).

## 리서치 요약 (파악 완료)
- 마케팅 페이지 패턴: 디자인 토큰(`@/lib/design-tokens` `T.*`) + 중앙정렬 + `max-width 600~1100px` + 11px 대문자 eyebrow + `FitText` 헤드라인 + 카드(`T.shadow`).
- 브랜드 메인색: **초록 `#38a848`** (`T.blue`가 실제 초록). 1:1 직강은 코드에 이미 **보라 `#5c4edc`** 로 구분됨(관리자 "1:1수업" 배지).
- 코드에 이미 **직강 인프라 존재**: `directClasses` 컬렉션(과목), **수업일지(lessonLogs)** — 부모가 수업일지를 열람하는 기능. → 홍보 셀링포인트로 활용 가능.
- 라우트: `src/app/on/` 폴더 → URL `/on`.

## 설계 결정 (검토해줘 — 아래 4개는 내 추천, 바꿀 거 있으면 코멘트)
1. **색인**: 홍보용이므로 **구글 색인 O** + `sitemap.ts`에 `/on` 추가 (momsaipack은 noindex였지만 이건 노출 목적)
2. **문의/신청 CTA**: 카카오 오픈채팅 링크 (momsaipack이 쓰는 방식) 우선 추천. → **오픈채팅 URL 필요**. 또는 `/signup` 연결도 가능.
3. **콘텐츠 방식**: v1은 **정적 데이터 파일**(`src/data/one-on-one.ts`)에 문구를 두고 페이지가 렌더 (단순·SEO 유리). 어드민 인라인 편집(서비스처럼)은 필요 시 다음 단계.
4. **강조색**: 보라 `#5c4edc`(1:1 아이덴티티) + 초록 브랜드 베이스 혼용. 페이지 전체를 보라 톤으로.

## 페이지 구조 (와이어프레임)

```
┌─ Hero ──────────────────────────────────────────┐
│  eyebrow: 1:1 DIRECT · 직강                       │
│  H1: (강사가 직접 가르치는 1:1) — FitText          │
│  sub: 한 줄 소개                                   │
│  [ 상담 신청 → ]  (보라 CTA)                        │
└──────────────────────────────────────────────────┘
┌─ Why (왜 1:1 직강인가) ── 카드 3~4개 ─────────────┐
│  구독형 자기주도와 다른 점: 강사가 직접 밀착 지도    │
│  예) 진단 맞춤 · 실시간 피드백 · 수업일지 공유        │
└──────────────────────────────────────────────────┘
┌─ 수업 소개 (What) ───────────────────────────────┐
│  과목/대상/형식 (예: 국어 독해 1:1, 초·중등)         │
│  진행 요일·시간·인원(1:1) 요약                       │
└──────────────────────────────────────────────────┘
┌─ 진행 방식 (How) ── 4스텝 ───────────────────────┐
│  1 상담 → 2 진단 → 3 1:1 수업 → 4 수업일지 피드백   │
│  (기존 lessonLogs/수업일지 기능을 강점으로 노출)      │
└──────────────────────────────────────────────────┘
┌─ 강사 소개 (Who) ────────────────────────────────┐
│  운영자 프로필: 경력·철학·한마디 (신뢰 핵심 섹션)     │
└──────────────────────────────────────────────────┘
┌─ 가격 (Pricing) ─────────────────────────────────┐
│  수업료·회차·환불/변경 등 안내                       │
└──────────────────────────────────────────────────┘
┌─ FAQ (선택) ─────────────────────────────────────┐
┌─ CTA 배너 ── [ 상담 신청 → ] ────────────────────┘
```

## 구현 태스크 (승인 후)
1. `src/data/one-on-one.ts` — 페이지 문구/스텝/FAQ/프로필/가격 상수 정의 → verify: 타입 통과
2. `src/app/on/page.tsx` — 서버 컴포넌트, metadata(title/description/OG, index 허용), 위 섹션 렌더 → verify: 빌드 시 `/on` prerender
3. 섹션 컴포넌트: 랜딩 패턴 재현(토큰 사용, 보라 강조). 재사용 최소화로 페이지 폴더 안에 배치 → verify: lint
4. `src/app/sitemap.ts` 에 `/on` 추가 (결정 1 승인 시) → verify: 빌드 sitemap에 포함
5. (선택) 랜딩 `/` 또는 Navbar에 `/on` 진입 링크 노출 여부 — 결정 필요
6. `npm run deploy` 후 라이브 `/on` 200 확인

## ⚠️ 너한테 필요한 콘텐츠 (이게 있어야 실제 문구를 채움)
빈칸이면 플레이스홀더로 먼저 만들고 나중에 교체 가능. 있는 만큼 주면 돼:
- [ ] **과목/대상**: 무슨 과목을, 몇 학년 대상으로 1:1 수업하나? (예: 중등 국어 독해)
- [ ] **한 줄 카피**: 헤드라인으로 쓸 핵심 메시지
- [ ] **강사 소개**: 경력/이력/수업 철학 (신뢰 섹션 — 제일 중요)
- [ ] **수업 방식**: 온라인/오프라인, 요일·시간, 1회 길이, 교재
- [ ] **가격**: 수업료(회당/월), 환불·변경 규정
- [ ] **문의 채널**: 카카오 오픈채팅 URL(또는 전화/카톡ID)
- [ ] **차별점 3~4개**: 왜 구독형 말고 직강인지
- [ ] (선택) FAQ 질문·답변, 후기/실적

## 미구현
아직 코드 작성 전 — 설계 검토 및 콘텐츠 대기 중.
