# plan: 캠퍼스 동숲 v1 — 하룻밤 슬라이스 (2026-08-10 밤)

> 근거: research.md (2026-08-10). /goal 지시("내일 아침까지 마무리")로 승인 절차를 생략하고
> 야간 자율 구현. 전면 R3F 재작성은 리스크가 커서 **기존 map.js 레벨 구조 위에 얹는다** —
> R3F 이전은 이후 슬라이스에서.

## 범위 (동숲 4요소를 기존 캠퍼스에 이식)

### 1. 동숲 룩 (야외)
- 곡면 월드: world 하위 재질(lam/flat)에 `onBeforeCompile`로 `mv.y -= mv.z²·k` 주입.
  야외 k≈0.0018(카메라가 멀어 데모보다 약하게), 실내 0. 아바타·이름표는 제외(중심부라 무시 가능).
- 잔디 색 유지하되 채도 살짝 올림, 떠다니는 구름 5~6개, 꽃 20송이 산포.
- 플레이어 두상 눌러주기: avatar-glb 머리 본/메시 비균등 스케일(x1.12 y0.95 z1.08) — 동숲 두상 실루엣.

### 2. 채집 (과일나무)
- 야외 나무 20그루 중 6그루를 과일나무로 승격(id 부여, 사과 3개 매달림).
- 존 `tree` 근접 → 프롬프트 "나무 흔들기"(Space/버튼) → 수관 흔들림 + 과일 낙하 → 근접 자동 줍기.
- 일일 리스폰: `picked[YYYY-MM-DD] = [treeId]` — 오늘 흔든 나무는 빈 수관. 자정 지나면 복원.

### 3. 인벤토리 · 벨 · 조합 · 매점
- 카탈로그 `lib/items.js`: apple(사과)·juice(사과주스) + FURNITURE 가구 전종을 아이템化.
  조합: 사과×3 → 주스. 상점가: 사과 팔기 100 / 주스 팔기 400 / 가구 사기 300~800벨.
- 저장 `store.js` 확장: 로그인 `users/{uid}.campus.inv`(맵)·`campus.bells`(수), merge 저장.
  게스트 `localStorage mp.inv.v1`. 규칙 변경 불필요(기존 campus 필드 자기쓰기 경로 그대로).
- HUD: 우상단 '가방' 버튼(벨 잔액 병기) → 패널: 아이템 목록 + 조합 버튼.
- 매점: 휴게실(lounge)에 카운터+점원 NPC('매점쌤') + 존 `shop` → 팔기/사기 2탭 모달.

### 4. 자습실 방 꾸미기 (기존 데이터 모델에 UI를 붙인다)
- study 룸 존 안에서 로그인 시 '방 꾸미기' 버튼 노출 → 편집 모드:
  - 인벤 가구 선택 → 바닥 탭 = 배치(0.5 그리드 스냅, ROOM_BOUNDS 클램프, 겹침 허용 안 함)
  - 놓인 가구 탭 = 선택 → 회전(90°)/치우기(인벤 복귀)
  - 저장 = saveRoom + 인벤 동기 저장. 취소 = 원복.
- 게스트는 버튼 없음(기존 정책: 방문자 저장 없음).

## 하지 않는 것 (다음 슬라이스)
- R3F 이전, 외부 에셋(Kenney) 도입, Blender 두상 리셰이프, 유저 간 거래(에스크로),
  Functions 검증 경제, 동물 주민 NPC, BGM. — research.md 로드맵대로 아침 승인 후.

## 검증 → 배포
1. `npm run build` 통과
2. 프리뷰 채널 `crossing-v1` 배포 → 헤드리스 브라우저에서 `__game` 훅으로
   채집→조합→판매→구매→배치 전 루프 플레이 검증 + 모바일 뷰포트 스크린샷
3. 라이브 배포 → 프리뷰 채널 즉시 삭제(임시호스팅 잔재 0 유지) → 커밋

## 태스크
- [완료] items.js + store.js 인벤·벨 저장
- [완료] 야외: 곡면 셰이더 · 과일나무 · 채집 · 장식
- [완료] 가방 HUD · 조합 · 매점 상점
- [완료] 자습실 배치 모드 (게스트 차단 확인 — 로그인 플로우는 헤드리스에서 검증 불가, 아침에 실계정 확인 권장)
- [완료] 두상 스케일 폴리시
- [완료] 빌드·프리뷰 검증·라이브 배포·커밋 (2026-08-11 새벽 라이브 반영, 프리뷰 채널 삭제됨)

## 검증 기록 (프리뷰 채널에서 헤드리스 플레이)
채집(존→흔들기→낙하→줍기 3개) → 재흔들기 차단 → 조합(주스) → 학생회관 입장 →
매점 존 → 팔기(100·전부) → 의자 구입 → 게스트 localStorage 저장·리로드 복원 →
게스트 방꾸미기 차단 — 전부 통과. 모바일(390px) 레이아웃 확인.
