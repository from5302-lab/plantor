# 상점 재설계 — CSS 아이템 + 기간제 부스터 + 레벨 베네핏

작성: 2026-08-05
상태: **제안 (검토 대기)**
실측 기준: 학생 22명 · 학습일 242일 · 19,774XP · 보유 12,308P

## 0. 왜 다시 짜나
아이템 아트를 픽셀 이미지로 만들려던 게 병목이었다. 그림 없이 **CSS만으로** 꾸미기를 만들면
지금 바로 열 수 있고, 유지비도 0이다. 레퍼런스는 디스코드 니트로 —
니트로가 파는 것도 대부분 **아바타 장식 · 프로필 색 · 배너 · 프로필 이펙트**로, 그림보다 스타일이다.

## 1. 경제 기준선 (실측)

| 지표 | 값 |
|---|---|
| 학생 1명 하루 | 약 **82XP → 16P** |
| 주 5일 기준 | 약 **80P/주** |
| 현재 보유 최대 | 정승원 2,618P |
| 22명 총 보유 | 12,308P |

→ **가격대**: 소품 150~300P(2~4주), 중급 500~800P(6~10주), 최상급 1,500P(반년).
지금 보유량이면 대부분 즉시 1~2개를 살 수 있어 "열자마자 살 게 있는" 상태다.

## 2. CSS 아이템 카탈로그 (그림 0장)

기존 슬롯 `background · frame · effect` 를 그대로 쓰고, 값만 CSS로 채운다.
`base/hair/outfit/hat/prop` 은 이미 이모지라 그림이 필요 없다.

### 2.1 배경 (background) — 니트로 "프로필 배너"
```css
/* 그라데이션 */
--bg: linear-gradient(135deg, #a8edea, #fed6e3);        /* 파스텔 */
--bg: linear-gradient(135deg, #0f2027, #2c5364);        /* 딥오션 */
/* 패턴 (반복 그라데이션 = 이미지 없이 무늬) */
--bg: repeating-linear-gradient(45deg, #f6f5f4 0 8px, #eae8e5 8px 16px);   /* 스트라이프 */
--bg: radial-gradient(#d7ecd9 1.5px, transparent 1.6px) 0 0/12px 12px;      /* 도트 */
/* 홀로그램 */
--bg: conic-gradient(from 180deg, #ffd6e7, #c1f0ff, #d9ffe1, #ffe9c1, #ffd6e7);
```
가격 200~700P · 12종 정도

### 2.2 테두리 (frame) — 니트로 "아바타 장식"
```css
/* 회전하는 무지개 링 — 니트로 애니 아바타 자리 */
border: 3px solid transparent;
background: linear-gradient(#fff,#fff) padding-box,
            conic-gradient(from var(--a), #38a848, #7cd992, #38a848) border-box;
animation: spin 3s linear infinite;   /* @property --a 로 각도 애니메이션 */

/* 이중 링 */ box-shadow: 0 0 0 2px #fff, 0 0 0 5px #38a848;
/* 발광 */   box-shadow: 0 0 0 2px #fff, 0 0 12px 2px rgba(56,168,72,.55);
/* 점선 */   border: 2.5px dashed #b45309;
```
가격 300~1,200P · 8종

### 2.3 이펙트 (effect) — 니트로 "프로필 이펙트"
CSS 애니메이션만. `prefers-reduced-motion` 가드 필수.
- **반짝임**: `::after` 를 작은 원 3개로 두고 `opacity/scale` 키프레임
- **떨어지는 잎**: 의사요소 2개 + `translateY + rotate` 무한
- **맥동 오라**: `box-shadow` 확산 반복
- **무지개 이름**: `background-clip:text` + 그라데이션 이동 → **니트로의 프로필 색**에 해당

가격 700~1,500P · 6종

### 2.4 이름 색 (신규 슬롯 `nameStyle`) — 니트로 핵심
피드에서 가장 눈에 띄는 자리. 경쟁심 자극이 가장 큰 아이템.
```css
color: #b45309;                                   /* 단색 */
background: linear-gradient(90deg,#38a848,#7cd992); -webkit-background-clip: text;  /* 그라데 */
```
가격 400~1,000P · 8종

> **주의**: 이름 색이 과하면 피드 가독성이 무너진다. 채도·명도 하한을 두고
> 배경 대비 4.5:1 미달 색은 카탈로그에 넣지 않는다.

## 3. 기간제 경험치 부스터

| 아이템 | 효과 | 기간 | 가격 |
|---|---|---|---|
| 집중 부스터 | XP ×1.5 | 3일 | 400P |
| 열공 부스터 | XP ×2 | 3일 | 700P |
| 폭주 부스터 | XP ×2 | 7일 | 1,400P |
| streak 보호권 | 연속 1일 결손 방어 | 1회 | 500P |

### 3.1 반드시 피해야 할 함정 — 멱등성
`awardRewards` 는 **같은 날짜·서비스를 하루에도 여러 번 재계산**하고 차액만 반영한다.
배수를 "지금 시각"으로 판정하면, 부스터가 만료된 뒤 재계산될 때 **이미 준 XP가 깎인다.**

→ 배수는 **그 날짜·서비스를 처음 적립할 때 확정해 원장에 박아두고**(`ledger.boostMult`),
재계산은 항상 저장된 값을 쓴다. 부스터가 중간에 끝나도 그날 기록은 흔들리지 않는다.

### 3.2 설계 원칙 — 품질 축은 건드리지 않는다
부스터는 **완료점·분량 보너스에만** 곱하고 품질점(Q)에는 곱하지 않는 안을 권한다.
안 그러면 "돈으로 산 배수"가 실력 차를 덮어 `plan-reward-system.md §1`의
"차이는 품질에서" 원칙이 무너진다.

### 3.3 피드 표기
부스터 적용분은 카드에 `×2` 배지를 달아 **부풀린 XP인지 구분**되게 한다.
안 그러면 학부모가 보는 "오늘 N XP"의 의미가 흐려진다.

## 4. 레벨 베네핏 사다리

지금은 레벨업이 포인트 100P뿐이라 밋밋하다. 레벨 자체에 혜택을 붙인다.

| 레벨 | 혜택 |
|---|---|
| Lv.5 | 이름 색 슬롯 해금 |
| Lv.10 | 포인트 적립 **+5%** · 배경 3종 해금 |
| Lv.20 | 이펙트 슬롯 해금 |
| Lv.30 | 포인트 적립 **+10%** · 상점가 5% 할인 |
| Lv.40 | 프레임 애니메이션 해금 |
| Lv.60 | 포인트 적립 **+15%** |
| Lv.80 | 전용 테두리(구매 불가, 레벨로만) |

**적립률 증가가 핵심**이다 — 레벨이 오를수록 꾸미기 속도가 빨라져 복리가 생긴다.
다만 상한(+15%)을 두어 후발 주자가 영원히 못 따라잡는 상황은 막는다.

## 5. 작업 순서

| # | 작업 | 상태 |
|---|---|---|
| 1 | 슬롯별 CSS 토큰 정의 + `AvatarView` 확장 | **[완료]** |
| 2 | 카탈로그 교체 | **[완료]** — 계획과 달라짐, §5.1 참고 |
| 3 | `nameStyle` 슬롯 신설 + 피드 반영 | **[완료]** |
| 4 | 부스터 — 원장에 배수 고정(§3.1), 품질 제외(§3.2) | 미착수 |
| 5 | 레벨 베네핏 (적립률·해금) | 부분 — 레벨 해금만(`minLevel`), 적립률 증가는 미착수 |
| 6 | 상점 재오픈 (`SHOP_OPEN = true`) | **[완료]** |

### 5.1 계획과 달라진 점 — 이모지 파츠 폐기

계획 §2 는 "`base/hair/outfit/hat/prop` 은 이미 이모지라 그림이 필요 없다"고 봤지만, **틀렸다.**
캐릭터가 식물(🌱🌵🍄)이라 머리도 몸도 없어서 옷을 얹을 자리가 없고, 아이템 이모지도
잘못 골라져 있었다(`💇` 는 머리카락이 아니라 "이발하는 사람", `👨‍🚀` 는 우주복이 아니라 우주인 전체).
결과적으로 무관한 이모지가 원 귀퉁이에 떠 있을 뿐 "꾸몄다"가 성립하지 않았다.

→ `hair · outfit · hat · prop` 슬롯을 폐기했다(구매 이력 0건이라 피해 없음).
남은 슬롯은 **`base · frame · nameStyle · background · effect`** 다.
그 대신 `base` 를 유니코드 식물 이모지 전체(26종)로 넓혀 "어떤 식물이 되느냐"를 정체성으로 삼았다.
니트로도 옷을 팔지 않고 테두리·이펙트·이름색·배너를 판다는 §0 의 레퍼런스와도 오히려 더 맞는다.

레벨 칭호에서는 식물 이모지를 뺐다 — 같은 🌱 이 "산 캐릭터"와 "레벨" 두 뜻이 되면
피드에서 잘못 읽힌다. 칭호는 이름 + 단계별 색만 쓴다.

## 6. 남은 결정 (부스터 착수 시)
- 부스터를 **품질점에도** 곱할지 (권장: 아니오)
- 부스터 표기(`×2`)를 학부모에게도 보일지 (권장: 예 — 신뢰 기준)

이름 색 피드 반영은 **예**로 결정해 적용했다.
