# 점수에 따른 경험치 차등 — plan

작성: 2026-08-07
상태: **[완료]** — 2026-08-07 승인 후 구현·배포
사전 조사: 실측 완료 (§1). 제안값은 실제 로그 139건에 돌려본 결과를 §5에 붙였다.

## 0. 한 줄 요약

점수에 따른 차등은 **이미 작동하고 있다.** 다만 ① 아무 데도 그렇게 보이지 않고,
② 클래스카드만 기준선이 실제 점수 분포와 어긋나 차이가 눌려 있다.
보이게 만들고, 기준선을 고치고, 잘한 날에 구간 보너스를 얹는다.

## 1. 지금 (실측)

산식: `XP = (기본 60 + 품질 0~40) × 연속배수 × 뱃지배수` — 품질이 아이가 가져온 점수다.

완료 로그 139건을 현재 산식에 그대로 통과시킨 결과:

| 서비스 | 완료 | 점수 P25 | 중앙 | P75 | 하위25% 평균 XP | 상위25% 평균 XP | 격차 |
|---|---|---|---|---|---|---|---|
| 클래스카드 | 77 | 84 | 91 | 95 | 93 | 100 | **8%** |
| 오토보카 | 17 | 42 | 70 | 88 | 61 | 107 | 75% |
| 매일국어 | 24 | 33 | 48 | 70 | 64 | 93 | 45% |
| 클래스5 | 21 | 63 | 78 | 89 | 68 | 100 | 47% |

**클래스카드만 8%다.** 기준선을 `60점=바닥 / 100점=만점`으로 잡아뒀는데
실제 점수는 P10이 이미 81점이다. 아무도 안 가는 60~80 구간에 기준선 절반을 쓰고 있어
실제로 쓰이는 폭이 84~100의 좁은 구간으로 눌렸다.

점수 분포(클래스카드 77건): `0~49: 2 | 70~79: 5 | 80~89: 26 | 90~94: 20 | 95~100: 24`

## 2. [완료] ① 보이게 만들기 — 이게 핵심이다

지금은 피드에도 학습현황에도 **"점수를 잘 받아서 더 받았다"는 말이 한 줄도 없다.**
아이 입장에서 XP는 그냥 매일 다른 숫자다. 모르는 보상은 동기가 되지 않는다.
차등이 이미 있는데 전달이 안 되고 있으므로, 산식보다 이쪽이 먼저다.

**피드 — 서비스 줄에 한 줄 추가**
```
📘 클래스카드                                    112 XP
   점수 95 → 경험치 +38 · 95점↑ 보너스 +20
   [문법] Unit 04 …  단어 95점 · 실전 100점
```

**학습현황(/learn) — 그날 XP가 어떻게 나왔는지 펼쳐서**
```
오늘 112 XP
  기본        60      과제 완료
  점수        +38     95점 (80점부터 오름)
  구간 보너스  +20     95점 이상
  연속 3일     ×1.1
```

`xpLedger.breakdown` 에 이미 다 들어 있다. 피드 문서(`feedEvents.services[]`)에만
근거 문자열이 없어서 거기에 한 필드를 얹는다.

## 3. [완료] ② 클래스카드 기준선 교정

```
지금:  60점 = 0점,  100점 = 40점
제안:  80점 = 0점,   99점 = 40점     ← 실측 P10 81 / P90 99 에 맞춤
```

다른 서비스가 쓰는 것과 같은 원칙이다 — **그 서비스의 P10을 바닥, P90을 만점으로.**
이러면 중앙값(91점)이 Q 0.58 근처에 놓여 다른 서비스와 눈금이 같아진다.

**대가를 분명히 적는다.** 잘 못 본 날 XP가 줄어든다. 84점 이하 구간 평균 **93 → 78 XP.**
차등을 세우기로 한 이상 피할 수 없는 방향이고, 아래 ③이 위쪽을 올려 전체 평균은 유지된다.
과거 XP는 **소급하지 않는다.**

## 4. [완료] ③ 점수 구간별 보너스

서비스마다 점수 분포가 딴판이라(클래스카드 중앙 91 vs 매일국어 중앙 48)
`90점 이상 +20` 같은 공통 기준은 불공평하다. 클래스카드는 절반이 받고 매일국어는 아무도 못 받는다.
**서비스별로 자기 분포의 상위 구간**을 기준으로 잡는다.

| 서비스 | +20 XP | +10 XP | +20 받는 비율 |
|---|---|---|---|
| 클래스카드 | 95점 ↑ | 90점 ↑ | 31% |
| 클래스5 | 90점 ↑ | 80점 ↑ | 24% |
| 오토보카 | 90점 ↑ | 80점 ↑ | 18% |
| 매일국어 | 80점 ↑ | 65점 ↑ | 13% |

대략 상위 4분의 1이 +20을 받는다. 숫자는 표 하나만 고치면 언제든 조정된다.

품질 점수(0~40)와 **따로** 얹는다. 완만한 비례에 문턱을 하나 더해
"조금만 더 하면 한 칸 올라간다"가 보이게 하는 것이 목적이다.

## 5. 셋을 합치면 (실제 로그 139건 시뮬레이션)

| 서비스 | 하위 25% | 상위 25% | 전체 평균 | 격차 |
|---|---|---|---|---|
| 클래스카드 | 93 → **78** | 100 → **119** | 97 → 98 | 8% → **53%** |
| 오토보카 | 61 → 61 | 107 → **123** | 85 → 91 | 75% → **102%** |
| 매일국어 | 64 → 64 | 93 → **108** | 76 → 80 | 45% → **69%** |
| 클래스5 | 68 → 68 | 100 → **118** | 86 → 93 | 47% → **74%** |

- 전체 평균은 0~8% 오른다. 레벨업 속도가 크게 흔들리지 않는다.
- 하루 상한(600)·서비스 상한(250)에 닿는 건은 시뮬레이션에서 **0건**.
- 줄어드는 건 클래스카드 하위 구간뿐이고, 그건 ②의 의도된 결과다.

## 6. 구현 범위

| 파일 | 변경 |
|---|---|
| `functions/src/rewards-config.ts` | `QUALITY_ANCHOR.classcard` 교정, `SCORE_TIERS` 표 신설 |
| `functions/src/rewards.ts` | `computeXp`에 구간 보너스 합산, `breakdown.tier` 기록 |
| `functions/src/rewards.ts` | `studySummary` — 서비스 줄에 근거 문자열 생성 |
| `functions/src/feed-events.ts` | `StudyEntry`에 근거 필드 하나 추가 |
| `src/components/community/feed-event-card.tsx` | 그 줄 렌더 |
| `src/components/learn/auto-result-card.tsx` | 학습현황에 XP 내역 펼치기 |
| `src/lib/rewards/catalog.ts` | 구간 표 클라이언트 미러(소개 페이지 표기용) |

## 7. 검증

1. `npm --prefix functions run build` · `npm run build`
2. `scripts/test-xp-tiers.mjs` 신설 — 서비스별 문턱 경계값(89/90, 94/95…)과
   상한 동작을 실제 로그 픽스처로 고정
3. 배포 전 139건 재시뮬레이션 — 위 표와 같은 수치가 나오는지 확인
4. 배포 후 다음 배치에서 피드 한 장 눈으로 확인

## 8. 안 하기로 한 것

- **차등 폭 자체를 키우기**(기본 60/품질 40 → 40/60): 못한 날 보상이 줄어드는 쪽이라
  저학년이 위축될 수 있다. ②③으로 격차가 이미 충분히 벌어져 필요 없다.
- **과거 XP 소급**: 그날 리워드를 다시 계산하면 연속 학습일이 흔들린다.
  새 산식은 배포 이후 적립분부터 적용한다.

## 9. 구현 결과 (2026-08-08)

배포 후 실제 완료 로그 139건 재시뮬레이션 (연속 배수 미적용 기준):

| 서비스 | 하위 25% | 상위 25% | 격차 | 평균 |
|---|---|---|---|---|
| 클래스카드 | 80 | 119 | 8% → **49%** | 99 |
| 오토보카 | 61 | 123 | **102%** | 90 |
| 매일국어 | 64 | 107 | **67%** | 79 |
| 클래스5 | 68 | 118 | **74%** | 93 |

서비스 상한(250) 도달 0건. 회귀 테스트 `scripts/test-xp-tiers.mjs` 18건.

구간 보너스는 품질 점수와 **함께** 괄호 안에 넣어 연속 배수·상한을 똑같이 받는다
(기획 시뮬레이션은 상한 밖에서 더했다 — 그러면 상한이 뚫린다). 그래서 격차가 53%가 아니라 49%다.

### 사고 기록 — 잘못 붙은 만회 계수
새 산식을 반영하려고 `runAutoVerifyNow?date=2026-08-07` 을 **다음 날** 호출했다.
`awardRewards` 는 `late = date < 오늘` 로 만회를 판정하므로, 제때 한 학습 18건에 ×0.7 이 붙어
XP 520 이 깎였다. `scripts/repair-late-factor.js` 로 되돌렸다(원장·아이 합계·피드 카드).

**만회 판정의 근거가 "학생이 언제 했는가" 가 아니라 "우리가 언제 계산했는가" 다.**
정규 배치는 늘 오늘만 돌려서 드러나지 않던 함정이다. 과거 날짜로 배치를 수동 재실행할 때 주의.

### 소급 범위 (기획과 달라진 점)
"새 산식은 배포 이후부터" 라고 적었지만, 위 재실행 때문에 **8/7 하루가 새 산식으로 다시 계산됐다.**
대부분 올랐고(만회 계수 복구 포함) 클래스카드 기준선 교정으로 한 명이 99 → 83 으로 내려갔다.
8/6 이전은 손대지 않았다.
