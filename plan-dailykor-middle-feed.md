# 중등 매일국어 피드 아웃풋 보강 — plan

작성: 2026-08-07
상태: **[완료]** — 2026-08-07 승인(③은 B안) 후 구현·배포
사전 조사: 실측 완료 (§1·§2)

## 0. 한 줄 요약

중등이 빈약한 이유는 **덜 긁어와서가 아니라, 긁어온 걸 피드에서 버려서**입니다.
스크래퍼는 지문마다 독해속도·훈련시간 3단계를, 하루마다 단계별 경험치를 이미 가져오는데
피드에 실리는 건 `정답률` 하나뿐입니다. 새 스크래핑 없이 **표시만 고치면** 초등과 같은 밀도가 됩니다.

## 1. 현황 (같은 날, 같은 서비스)

`feedEvents` 실제 문서 (2026-08-07):

```
초5 임○주 · 매일국어 완료
  38회차  [오전 9:35 ~ 오전 9:45]  국어
    단어 65점 · 교과서 72점 · 실전 90점 · 최초 78점      ← stat 5개
  38회차  [오전 9:45 ~ 오전 9:57]  과학
    단어 65점 · 교과서 72점 · 실전 65점 · 최초 68점

중2 이○민 · 매일국어 경험치 10/30
    비문학 > 과학
    정답률 33%                                          ← stat 1개
```

밀도 차이 **5 : 1**. 회차도, 시각도, 시간도 없습니다.

## 2. 이미 수집 중인데 피드에서 버려지는 값 (실측 74건 기준)

같은 학생 같은 날의 `learningLogs.scrapedData.detail` 원본:

```json
{ "passages": [{ "passageCode": "A000253", "type": "비문학 > 과학",
                 "accuracy": "33%", "readingSpeed": "1265/분",
                 "readingChars": 1033, "readingElapsed": "49초",
                 "prepTime": "2분 18초", "readingTime": "2분 6초", "practiceTime": "56초" }],
  "xpGot": 10, "xpMax": 30,
  "stepXp": [{"준비":"3/10"},{"독해":"2/12"},{"실전":"5/8"}],
  "recommendedSpeed": 600 }
```

| 값 | 수집 로그 | 피드 표시 | 개인 학습현황(/learn) |
|---|---|---|---|
| 정답률 | 74/74 | ✅ | ✅ |
| 독해속도 + 근거(자·초) | **74/74** | ❌ | ✅ (추천치 비교 칩까지) |
| 훈련시간 준비·독해·실전 | **74/74** | ❌ | ✅ |
| 단계별 경험치 stepXp | **24/74** | ❌ | ❌ |
| 지문코드 | 74/74 | ❌ | ✅ (캡션) |
| 획득 경험치 | 74/74 | note 한 줄 | ✅ |

- 중등 지문 로그 74건 / 학생 4명. 그중 **45건이 지문 2개 이상**입니다.
- `stepXp`는 파싱을 나중에 붙여 과거 로그엔 없습니다 → **있을 때만** 표시(없으면 조용히 생략).

## 3. 보강안 — 4가지, 따로따로 채택 가능

전부 `functions/src/rewards.ts`의 `studySummary()` 한 함수 안입니다.
피드 카드는 `{name, value}` 칩을 그대로 그리므로 **클라이언트 수정 없음**.

### ① [완료] 학습시간 — 초등과의 가장 큰 격차
`prepTime + readingTime + practiceTime` 합계를 지문 줄에 붙입니다.

> 비문학 > 과학 — `정답률 33%` `학습 5분 20초`

초등은 시계 시각(9:35~9:45)이 있고 중등은 없습니다. 중등 리포트는 시계 시각을 주지 않지만
**걸린 시간**은 줍니다 — "얼마나 붙잡고 있었나"라는 학부모의 질문에는 오히려 이쪽이 직접 답합니다.

### ② [완료] 단계별 경험치 — 초등의 단어/교과서/실전에 대응
`stepXp`를 하루 요약 줄로 한 줄 더 답니다.

> 오늘의 학습 — `준비 3/10` `독해 2/12` `실전 5/8`

지금은 `경험치 10/30`만 보여 **어디서 깎였는지** 알 수 없습니다. 초등이 세 칸으로 보여주는 것을
중등도 세 칸으로 보여주게 됩니다. 위 예시는 "독해를 건너뛰었다"가 한눈에 읽힙니다.

지문 단위가 아니라 하루 단위 값이라 지문 줄에 붙이면 안 됩니다(지문 2개면 같은 값이 두 번 찍힙니다).
`items` 맨 끝에 요약 줄 하나로 넣습니다.

### ③ [완료] 독해속도 — **B안 채택** (숫자만 피드에, 판정은 /learn)
> 비문학 > 과학 — `정답률 33%` `독해속도 1,265자/분 (너무 빠름)` `학습 5분 20초`

- 매일국어 리포트가 스스로 밝힌 추천치 600자/분의 2배를 넘으면 리워드에서도 이미 **XP를 절반으로** 깎고 있습니다([rewards.ts:137](functions/src/rewards.ts:137)).
- 즉 판정은 우리가 새로 만드는 게 아니라 **이미 적용 중인 감점을 학부모에게 알려주는 것**입니다. 위 학생이 10/30을 받은 진짜 이유가 이겁니다.
- 다만 **피드는 공개 화면**입니다. "너무 빠름"은 다른 집도 봅니다.
  - **A안**: 숫자와 판정 모두 공개 (`1,265자/분 (너무 빠름)`)
  - **B안**: 숫자만 공개, 판정은 `/learn` 개인 화면에만 (지금처럼)
  - **C안**: 피드엔 넣지 않음
  - 제 의견은 **B안**입니다. 정답률 0%가 이미 공개되고 있으니 숫자 자체는 새로운 노출이 아니고, 낙인이 되는 건 숫자가 아니라 딱지 쪽입니다. 판정은 집에서 보는 화면에서 충분히 전달됩니다.

### ④ [완료] 지문 순번을 회차 자리에 (지문코드는 넣지 않음)
초등 줄의 `38회차` 자리(`kind`)가 중등은 비어 있습니다. `A000253`을 넣으면 레이아웃이 초등과 같아집니다.
다만 학부모에게 의미 없는 문자열이라 **넣지 않는 쪽**을 권합니다. 대신 지문이 둘 이상일 때 `1지문`·`2지문`을 넣으면 자리도 채우고 뜻도 있습니다(45/74건이 해당).

### 채택 시 최종 모습 (①②④, ③은 B안)
```
중2 이○민 · 매일국어 경험치 10/30
  1지문   비문학 > 과학
    정답률 33% · 독해속도 1,265자/분 · 학습 5분 20초
  오늘의 학습
    준비 3/10 · 독해 2/12 · 실전 5/8
```
stat 1개 → 6개. 초등(5개)과 같은 급이 됩니다.

## 4. 구현 범위

| 파일 | 변경 |
|---|---|
| [functions/src/rewards.ts](functions/src/rewards.ts:324) | `studySummary()`의 dailykor 중등 분기 — 지문 줄에 시간·속도 stat 추가, 끝에 stepXp 요약 줄 추가 |
| — | 한국어 시간 문자열("2분 18초") 합산 헬퍼 1개. `/learn` 쪽 `parseKoTime`·`formatKoTime`과 같은 규칙 |
| 클라이언트 | **없음** (칩은 범용 렌더) |
| 스크래퍼 | **없음** (이미 다 긁고 있음) |

`items.slice(0, 6)` 상한에 걸립니다 — 지문 3개 + 요약 줄이면 잘립니다. 요약 줄은 상한 밖으로 빼거나 상한을 7로 올립니다.

## 5. 검증

1. `npm --prefix functions run build` → 타입 통과
2. 실제 로그 74건을 `studySummary()`에 통과시키는 스크립트로 **전/후 items 비교 출력** — 배포 전에 결과물을 눈으로 확인
3. 배포 후 다음 배치(09·13·17·21시)에서 중2·중3 카드 확인
4. **과거 카드는 그대로입니다** — 피드 문서는 기록 시점 값이 박혀 있어서, 새 형식은 오늘 이후 적립분부터 보입니다. 과거까지 맞추려면 재계산 스크립트가 따로 필요합니다(원하시면 따로 씁니다).

## 6. 조사만 해둔 것 (이번 범위 밖)

### 중등 시계 시각 — [조사 완료 2026-08-07] **경로 없음. 다시 찾지 말 것**
초등은 앱 API(`learning_history/academy`)에서 `start_date`·`end_date`를 받아 9:35~9:45를 찍는다.
중등에 대응하는 경로가 있는지 학원 계정으로 읽기 조사했고, 네 갈래 모두 막혔다.

| 확인 | 결과 |
|---|---|
| `ajax_calendar_dailyreport` 응답의 시:분 패턴 | 0건 — 시각이 응답에 없다 |
| `/academy/student/studylist/{idx}` 외 3종 | 404 (초등 전용 경로) |
| 학원 메뉴 전체 | 리포트는 `student/sreport`·`elementary/sreport` 둘뿐 |
| `sreport.js`의 모든 엔드포인트 | 지문·어휘·교재뿐, 시각 관련 없음 |

결정적 근거: 초등 경로에 **중등 idx를 넣으면 토큰은 나오지만** 학습이력이 빈 배열이다.
```
GET /api/learning_history/academy/196713 → {"success":true,"data":{"learning_history":[]}}
```
토큰이 학생에 묶여 있고 그 테이블에 중등 학생의 행 자체가 없다. 초등은 앱에서 회차를 진행해
시작·종료가 남지만 중등은 리포트가 지문 매트릭스라 시각을 적는 자리가 없는 구조로 보인다.

남은 가능성은 **학생 본인 계정**뿐인데, 학생 개개인의 비밀번호를 모아야 해서
학원 계정 하나로 전원을 조회하는 지금 구조와 맞지 않는다. 추진하지 않는다.
→ 중등은 §3①의 **학습시간**이 최종 답이다.

### [완료 — 초등만] 곁다리 발견 — 매일국어 학생은 얼리버드·올빼미 뱃지를 못 땁니다
`얼리버드`(7시 전)·`올빼미`(23시 후) 판정은 `units[].startHour`로 하는데
([rewards.ts:443](functions/src/rewards.ts:443)), 오토보카·클래스카드·클래스5 스크래퍼만 이 값을 채웁니다.
매일국어는 초등·중등 모두 채우지 않아 **매일국어만 하는 학생은 두 뱃지의 조건에 아예 도달할 수 없습니다.**
장착 효과(`아침 학습 +30%`)도 마찬가지로 안 걸립니다.
초등은 `elementary[].startAt`이 있어 바로 고칠 수 있고, 중등은 위 시계 시각 조사에 달려 있습니다.
이번 건과 별개이니 따로 결정해 주세요.

## 7. 구현 결과 (2026-08-07)

- 지문 줄 stat: `정답률` 하나 → `정답률 · 독해속도 · 학습시간`
- 하루 요약 줄 `단계별 경험치` 추가 (stepXp 있는 로그만)
- 요약 줄 라벨은 계획의 "오늘의 학습" 대신 **"단계별 경험치"** — "오늘의 학습"은 매일국어 파트 이름이라
  지문 줄 옆에 놓이면 무엇의 숫자인지 읽히지 않는다.
- 얼리버드·올빼미: 시각 출처를 `units[].startHour` → `startHours(scraped)`로 넓혀
  초등 `elementary[].startAt`도 본다. **중등은 리포트에 시계 시각이 없어 여전히 대상 밖.**
- 회귀 테스트: `node scripts/test-dailykor-summary.mjs` (10건)
- 실측 스윕: 중등 로그 74건 전부 개선, stat 합계 **119 → 429**, 6항목 상한에 걸린 로그 0건
