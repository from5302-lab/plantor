# 기획 — 캠퍼스에 높이 붙이기

근거: [research-campus-jump.md](research-campus-jump.md)

> **결정(2026-08-12)**: A — 곡면을 완전히 껐다. 아래 선택지는 기록으로 남긴다.

## 먼저 정할 것 하나

**곡면(동물의 숲 지평선)을 정말 끌 것인가?**

- **A. 완전히 끈다** — 점프맵이 제대로 된다. 지평선 룩은 사라진다.
- **B. 3분의 1로 줄인다**(0.0015 → 0.0005) — 지평선 느낌이 옅게 남고,
  20m 밖 어긋남이 60cm → 20cm. 가까운 데서 뛰어 노는 정도면 안 티 난다.
- **C. 야외는 곡면 유지, 점프는 실내·전용 레벨에서만**

기본값은 **B** 로 잡았다. 완전히 끄는 건 되돌리기 쉬우니(한 줄), 실제로
뛰어 보고 걸리적거리면 그때 0 으로 내리면 된다.

---

## 1단계 — 충돌 상자에 높이

- [완료] 1-1. `resolve` 가 쓰는 상자 모양을 `{minX,maxX,minZ,maxZ,top}` 으로 넓힌다.
      `top` 이 없으면 `Infinity`(지금과 같은 동작) → **기존 코드가 안 깨진다**
      → 검증: 아무 것도 안 고친 상태에서 지금과 똑같이 움직인다
- [완료] 1-2. 이미 높이를 아는 곳에 `top` 을 넘긴다 — 건물(`bh`) · 소품(`p.h`) ·
      실내 벽(1.5) · 필러 · 과일나무(2.2)
- [완료] 1-3. `drawDecor` 는 `decorSize().h` 를 `top` 으로 넘긴다(꾸미기 배치 전부)
- [완료] 1-4. 울타리·화분은 모델에서 실측해 상수로 박고 근거를 주석에 남긴다
      → 검증: 콘솔에서 상자 목록을 찍어 `top` 이 전부 유한한지 본다

## 2단계 — 중력과 착지

- [완료] 2-1. `P.y`, `P.vy` 추가. 중력 `G = 18`(0.7m 점프가 0.5초에 끝나는 값)
- [완료] 2-2. `groundAt(x, z)` — 발밑 상자 중 `top` 이 가장 높은 것. 없으면 0
- [완료] 2-3. 매 프레임 낙하·착지. 착지 순간 `vy = 0`
- [완료] 2-4. `doJump` 는 **땅에 있을 때만** 뛴다(지금은 `jumpT` 로 막는다)
      → 검증: 상자 위에 올라서면 발이 윗면에 붙고, 걸어 나오면 떨어진다

## 3단계 — resolve 가 높이를 본다

- [완료] 3-1. 상자의 `top` 이 발보다 낮으면 통과(위를 걷는 중)
- [완료] 3-2. `top - 발 <= 0.35m` 면 자동으로 올라선다(계단 오르기)
- [완료] 3-3. 그 밖에는 지금처럼 민다
      → 검증: 낮은 단은 걸어서 오르고, 높은 벽은 못 넘고, 상자 위는 걸어 다닌다

## 4단계 — 딸려 오는 것

- [완료] 4-1. 카메라 `camLook` 이 `P.y` 를 따라간다(안 그러면 올라섰는데 화면이 그대로)
- [완료] 4-2. 펫이 주인 높이를 따라간다 — 착지면을 따로 계산한다
- [완료] 4-3. 네트워크 좌표에 `y` 추가. 옛 클라이언트가 안 보내면 0 으로 읽는다
- [완료] 4-4. 점프 중에는 곡면 기준점(`FOCUS`)이 흔들리지 않게 발 높이를 쓴다
      → 검증: 두 탭으로 접속해 한쪽이 상자 위에 서면 다른 쪽에서도 위에 보인다

## 5단계 — 안전장치

- [완료] 5-1. 울타리 `top = 4m` — 상자를 쌓아 마을 밖으로 못 나가게
- [완료] 5-2. `FLAT`(러그·타일·꽃)은 계속 충돌 제외 — 밟고 올라서는 물건이 아니다
- [완료] 5-3. 실내 벽 `top = 3m` — 허리 벽을 뛰어넘어 방 밖으로 나가는 것 방지
- [완료] 5-4. 곡률을 `CURVE_K` 상수 하나로 조절 가능하게 두고 0.0005 로 시작
      → 검증: 상자를 계단처럼 쌓아 울타리를 넘으려 해 본다(못 넘어야 한다)

## 6단계 — 마무리

- [완료] 6-1. 로딩 힌트에 "F 로 뛴다. 낮은 건 걸어서 오른다" 반영
- [완료] 6-2. 라이브에서 확인 → 커밋 → 푸시

---

## 안 하는 것

- 떨어져서 죽기 / 리스폰 — 구덩이가 없어 떨어질 데가 없다
- 이동 발판·스프링·가시 — 점프맵 **전용 레벨**을 만들 때 얘기다
- 벽 타기·이단 점프 — 지금 필요 없다

## 되돌리기

곡률은 상수 하나, `top` 은 없으면 무한대로 동작한다. 1단계까지만 넣고
멈춰도 지금과 똑같이 움직인다 — **어느 단계에서 멈춰도 망가지지 않는다.**


---

## 검증 기록 (2026-08-12)

라이브에서 `window.__game.COLLIDERS` 를 읽어 규칙을 검산했다.

```
콜라이더 17개, top 이 없는 것 0개
top 값: 0.4(벤치) · 0.71(화분) · 2.4(과일나무) · 3.1(소품) · 3.8(건물) · 4(울타리)

벤치(3.8, 2.2) 땅에서   → ground 0,    막힘 O   (0.40 > STEP_UP 0.35, 걸어선 못 넘는다)
벤치 위 0.5m 에서       → ground 0.40, 막힘 X   (뛰어오르면 윗면에 선다)
건물(0, -9.5) 땅에서    → ground 0,    막힘 O   (벽이다)
빈 잔디(6, 4)           → ground 0,    막힘 X
```

점프·낙하는 화면에서 확인했다(공중에 뜬 자세 → 착지).

⚠ 브라우저 탭이 백그라운드면 `requestAnimationFrame` 이 멈춰 캐릭터가 아예
  안 움직인다. 검증 중에 이걸 코드 버그로 한참 쫓았다 — `document.hidden` 부터 볼 것.
