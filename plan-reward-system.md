# 뱃지 · XP · 리워드 시스템 — plan

작성: 2026-07-29
상태: **기획 검토 대기 (사용자 승인 전 코딩 금지)**
근거 데이터: 2026-07-29 전 학생 실측 스크래핑 (오토보카 2개월/유닛 97건, 클래스카드 14일/문법 26유닛, 매일국어 상세 41건, 클래스5 14일/배정 211건)

## 0. 한 줄 요약
자동인증으로 이미 긁고 있는 점수·정답률을 **XP(레벨) + 포인트(상점) + 뱃지(자랑)** 세 갈래로 환산해, 학생이 "완료했다"가 아니라 **"얼마나 제대로 했나"로 성장**하게 만든다.

## 1. 설계의 출발점 (실측이 말해준 것)

| 관측 | 함의 |
|---|---|
| 클래스5 활동 score는 완료 시 **전부 100** | 완료 기반 XP는 변별력 0 → 품질 축이 반드시 필요 |
| 오토보카 하루 포인트가 33~39P에 몰림 | 외부 포인트를 그대로 XP로 쓰면 전원 동일 |
| 오토보카 **스펠 1회정답률 13~100%(중앙 71)** | 여기서 실력 차이가 드러남 |
| 매일국어 **달성률 10~100%(중앙 52)** | 유일하게 0~1로 자연 정규화됨 → 기준축 |
| 클래스5 **카드 1회정답률 7~100%(중앙 70)** | 클래스5의 실질 품질 지표 |
| 매일국어 독해속도 중앙 1,154자/분 (추천 600) | 절반 이상이 지문을 안 읽고 넘김 → 교정 대상 |
| 클래스카드 어휘/본문 14일간 5건, 듣기 1건 | 표본 부족 → 품질 고정값으로 시작 |

**원칙**: 완료는 "출석점수"로 후하게, 차이는 품질에서. 못하는 학생도 **성장 뱃지**로 딸 게 있어야 한다.

## 2. XP 산식

기준 단위: **서비스 1개 · 하루 1회 정상 학습 = 100 XP**

```
XP = (완료점 + 품질점) × 만회계수 × 연속배수 + 분량보너스
  완료점   : 완료 60 / 진행중 20 / 시작전 0
  품질점   : Q(0~1) × 40
  만회계수 : 정시 1.0 / 만회(late) 0.7
  연속배수 : 1.0 ~ 1.5 (streak)
  분량보너스: 표준량 초과 1건당 +15 (서비스당 하루 최대 +45)
상한: 서비스당 하루 250 XP · 학생당 하루 600 XP
```

### 2.1 서비스별 품질 Q (실측 분포로 앵커링)

| 서비스 | 품질 지표 | Q=0 | Q=1 | 실측 중앙 → Q |
|---|---|---|---|---|
| 오토보카 | 스펠(write) 1회정답률 | 40% | 90% | 71% → **0.62** |
| 클래스카드 · 문법 | 유닛 평균점수 | 60점 | 100점 | 91 → **0.78** |
| 클래스카드 · 듣기 | 테스트 점수 | 50점 | 100점 | 80 → **0.60** |
| 클래스카드 · 어휘/본문 | (표본 부족) 고정 | — | — | **0.50** 고정 |
| 매일국어 | 하루 xp 달성률 | 20% | 85% | 52% → **0.49** |
| 클래스5 | 카드 1회정답률 | 40% | 90% | 70% → **0.60** |

- **속도 페널티(매일국어)** — *사용자 확정: 유예 없이 즉시 적용*: 지문 평균 독해속도 > 추천치×2 → Q ×0.5. 실측 중앙이 이미 추천치의 약 2배라 **도입 초기에 절반가량이 페널티를 받는다**. 이건 버그가 아니라 의도된 신호이므로, 카드에 `너무 빨라요 — 지문을 읽으면 XP가 2배` 문구를 함께 띄워 원인을 알려준다
- **표준량**: 오토보카 1유닛 / 클래스카드 1유닛 / 매일국어 지문 2개 / 클래스5는 배정분 전부(초과 개념 없음, 대신 과거 만회분에 분량보너스)

### 2.2 연속(streak) 배수
| 연속일 | 3일 | 7일 | 14일 | 30일+ |
|---|---|---|---|---|
| 배수 | ×1.1 | ×1.2 | ×1.3 | ×1.5 |

- 정의: **그날 배정된 자동인증 서비스를 전부 완료한 날**의 연속. 배정이 없는 날(휴일 등)은 연속을 끊지 않고 유지.
- 상점의 `streak 보호권`으로 1일 결손 방어 가능.

### 2.3 예상 산출량 (설계 검증용)
- 서비스 1개 정상 학습 ≈ **80~100 XP**, 구독 2~3개 학생의 하루 ≈ **200~300 XP**
- 주 5일 기준 주 1,000~1,500 XP → 1년(40주) 약 40,000~60,000 XP

## 3. 레벨 & 칭호

| 구간 | 레벨당 필요 XP | 누적 |
|---|---|---|
| Lv 1→10 | 500 | 4,500 |
| Lv 10→30 | 1,500 | 34,500 |
| Lv 30→60 | 4,000 | 154,500 |
| Lv 60+ | 8,000 | — |

초반 2주 안에 Lv 10 도달하도록 설계(초기 이탈 방지). 칭호는 plantor 식물 테마 9단계:

| 칭호 | 씨앗 | 새싹 | 떡잎 | 줄기 | 잎새 | 꽃봉오리 | 개화 | 열매 | 큰나무 |
|---|---|---|---|---|---|---|---|---|---|
| 레벨 | 1–4 | 5–9 | 10–19 | 20–29 | 30–39 | 40–49 | 50–59 | 60–79 | 80+ |

**XP는 절대 감소하지 않는다** (레벨 = 누적 기록). 소비되는 건 포인트뿐.

## 4. 포인트 & 내부 상점 (아바타 꾸미기)

**사용자 확정**: 실물 보상 없음. 상점은 **플랜토 안의 디지털 아이템**(아바타 의상·스킨 등)만 판매하며, 구매는 **전액 자동 처리**(어드민 승인 절차 없음).

### 4.1 적립
- `포인트 = round(XP × 0.2)` → 하루 약 40~60P, 주 250~300P
- **뱃지 보너스**: 일반 50P / 희귀 150P / 영웅 400P / 전설 1,000P → 히든 뱃지 발견이 곧 꾸미기 재화가 되게 해 루프를 닫는다
- **레벨업 보너스**: 레벨당 100P
- XP는 감소하지 않고, 소비되는 건 포인트뿐

### 4.2 아이템 카테고리

| 카테고리 | 예시 | 가격대 |
|---|---|---|
| 아바타 베이스 | 새싹이 캐릭터 종류, 표정, 헤어 | 300~800P |
| 의상 | 상의·하의·한벌옷, 교복, 시즌 코스튬 | 300~1,200P |
| 액세서리 | 모자, 안경, 가방, 손소품, 펫 | 150~600P |
| 배경 | 프로필/공유카드 배경 | 200~800P |
| 프레임 | 아바타·뱃지 테두리 | 150~500P |
| 이펙트 | 레벨업·뱃지 획득 시 파티클 | 600~1,500P |

주 250~300P 적립 기준으로 **액세서리는 며칠, 의상 한 벌은 1~2주, 전설급 이펙트는 한 달**이 걸리는 속도다.

### 4.3 해금 방식 3종
1. **포인트 구매** — 상점의 기본 경로
2. **레벨 해금** — 특정 레벨 도달 시 구매 가능해지는 아이템(칭호 테마 배경: 떡잎/줄기/개화/큰나무 등). 레벨업에 실질 보상을 붙인다
3. **뱃지 전용 보상** — 특정 히든 뱃지를 따야만 얻는 아이템 (예: `제대로 읽었다`(전설) → 전용 프레임, `3만 클럽` → 게임 트로피 소품). **돈으로 못 사는 아이템**이라 자랑 가치가 가장 크다

### 4.4 아바타와 공유의 연결
착용 중인 아바타는 **뱃지 공유 카드(§5.7)에 그대로 렌더**된다. 꾸민 걸 남에게 보여줄 창구가 뱃지 자랑이므로, 꾸미기 동기와 자랑 동기가 서로를 밀어준다.

## 5. 뱃지 시스템 — **히든 업적 방식**

### 5.0 설계 원칙 (사용자 확정)
> "그런 뱃지가 있는 줄도 모르고 막 했는데 어느 날 뱃지가 생기는 거지"

- **조건을 미리 공개하지 않는다.** 뱃지함에는 획득한 것만 보이고, 미획득분은 `???` 실루엣으로 **개수만** 표시한다("숨은 뱃지 24개가 더 있어요"). 조건 문구는 획득하는 순간 처음 공개된다.
- **판정은 학습 데이터에서 자동으로.** 학생이 뭘 눌러 신청하는 게 없다. 자동인증이 돌 때마다 조건이 검사된다.
- 예외로 **성실 계열 4개만 공개**한다(연속 3·7·30·100일). 전부 숨기면 붙잡을 목표가 하나도 없어 오히려 동력이 죽는다.
- 희귀도: 일반=대부분 딴다 / 희귀≈상위 25% / 영웅≈상위 5% / 전설=극히 드묾. 모두 §1 실측 분포로 배정했다.
- **모든 조건은 실제로 수집되는 필드로만 판정한다.** 아래 표의 "판정 근거"가 그 필드다.

### 5.1 오토보카 (히든 10)
| 뱃지 | 조건 | 판정 근거 | 희귀도 |
|---|---|---|---|
| 3연속 만점 | 테스트 100점이 3회 연속 | `test_score_list` 이력 누적 | 영웅 |
| 한 방에 | 유닛 첫 테스트가 100점 | `test_score_list[0]` | 희귀 |
| 퍼펙트 스펠 | 스펠 전 단어를 한 번에 정답 | `write_list[].try_cnt` 전부 1 | 희귀 |
| 오답 소탕 | 그날 오답복습을 남김없이 끝냄 | `wrong_data.end_cnt == now_cnt` | 희귀 |
| 포인트 폭발 | 하루 포인트 80P 돌파 | `point_list` 합 (실측 p75 39, max 106) | 영웅 |
| 스피드런 | 10분 안에 유닛 완료 + 테스트 90↑ | `unit_duration` + `test_score_list` | 희귀 |
| 뚝심 | 한 유닛에 60분 이상 | `unit_duration` (실측 max 153분) | 희귀 |
| 승급 | 교재 권수가 올라간 첫 학습 | `book_short_name` 변화 | 일반 |
| 리뷰 정복 | 리뷰 유닛 평균 95점 이상 | `report_name`에 '리뷰' + 점수 | 희귀 |
| 포기 안 함 | 4번 이상 틀린 단어를 끝내 정답 | `try_cnt ≥ 4 && correct_cnt > 0` | 일반(성장형) |

### 5.2 클래스카드 (히든 7)
| 뱃지 | 조건 | 판정 근거 | 희귀도 |
|---|---|---|---|
| 올클리어 | 한 유닛 전 단계 100점 | 엑셀 단계 점수 전부 100 | 영웅 |
| 실전 강자 | 실전 문제 100점 | 단계 [실전 문제] (실측 중앙 70) | 영웅 |
| 서술형 정복 | 서술형 문제 90점 이상 | 단계 [서술형 문제] (0점 사례 흔함) | 희귀 |
| 오답 청소부 | 누적오답 100점 | 단계 [누적오답] | 희귀 |
| 개념 마스터 | 개념 톡 3유닛 연속 100점 | 단계 이력 누적 | 희귀 |
| 장기전 | 60분 이상 학습 + 평균 90점 이상 | 학습시작~종료 + 평균 | 희귀 |
| 다시 도전 | 듣기 오답 테스트 점수가 상승 | `95 → 100` 형태 파싱 | 희귀 |

### 5.3 매일국어 (히든 8)
| 뱃지 | 조건 | 판정 근거 | 희귀도 |
|---|---|---|---|
| 완벽한 하루 | 하루 획득 xp = 최대 xp | 총 xp (실측 달성률 중앙 52%) | 영웅 |
| 준비 만점 | 준비훈련 10/10 | 단계별 xp | 희귀 |
| 독해 만점 | 독해훈련 12/12 | 단계별 xp | 영웅 |
| 실전 만점 | 실전대비 8/8 | 단계별 xp | 희귀 |
| **제대로 읽었다** | 독해속도가 추천치 0.8~1.5배 + 실전 정답률 80%↑ | 독해속도 + 정답률 (실측상 극히 드묾) | 전설 |
| 쌍지문 | 하루 지문 2개 모두 완료 | 지문 수 | 일반 |
| 어휘 완전정복 | 어휘 한 분류의 전 세트 완료 | `ajax_voca_status` 진행 N/N | 희귀 |
| 어휘 퍼펙트 | verygood 세트 5개 연속 | 세트 등급 class | 희귀 |

### 5.4 클래스5 (히든 7)
| 뱃지 | 조건 | 판정 근거 | 희귀도 |
|---|---|---|---|
| 무결점 | 카드 1회정답률 100% | `card_list[].try_cnt/is_correct` | 희귀 |
| 전 단계 클리어 | 그 과제의 모든 활동 완료 | `is_s1~is_s16` | 일반 |
| 3만 클럽 | 문법 게임 30,000점 돌파 | `activity 7` score (실측 중앙 32,200) | 희귀 |
| 신기록 | 게임 활동 개인 최고점 경신 | 개인 최고 기록과 비교 | 일반(반복) |
| 삼종 제패 | 하루에 movie·book·grammar 전부 완료 | `movie_type` | 영웅 |
| 집중 완주 | 5분 안에 완료 + 1회정답률 90%↑ | `total_duration` + 카드 정답률 | 희귀 |
| 마라톤 | 하루 총 학습 30분 이상 | `total_duration` 합 (과제당 중앙 9분) | 일반 |

### 5.5 크로스 서비스 (공개 4 + 히든 9)
| 뱃지 | 조건 | 공개 여부 | 희귀도 |
|---|---|---|---|
| 첫걸음 / 일주일의 힘 / 한 달 개근 / 100일의 기적 | 3·7·30·100일 연속 | **공개** | 일반→전설 |
| 올클리어 | 하루 구독 서비스 전부 완료 | 히든 | 희귀 |
| 퍼펙트 위크 | 7일 연속 올클리어 | 히든 | 전설 |
| 얼리버드 | 오전 7시 이전에 학습 시작 | 히든 | 희귀 |
| 올빼미 | 밤 11시 이후 학습 | 히든 | 일반 |
| 주말 전사 | 토·일 모두 학습 | 히든 | 희귀 |
| 따라잡기 | 밀린 과제 3건 만회 | 히든 | 희귀 |
| 껑충 | 품질 지표가 전주 대비 +15%p | 히든 | 희귀(성장형) |
| 반전 | 매일국어 미흡 → 최우수 | 히든 | 영웅 |
| 수집가 | 뱃지 10개 / 25개 달성 | 히든 | 희귀/영웅 |

> 시간대 뱃지는 오토보카 `unit_start`, 클래스카드 `학습시작`, 클래스5 `last_ts`로 판정한다(매일국어는 시각 정보가 없어 제외).

### 5.6 "어느 날 갑자기" 연출
1. **즉시 발견**: 클릭 인증으로 조건이 걸리면 결과 카드 위에 0.6초 뒤 카드가 뒤집히며 등장 — `히든 뱃지 발견!` + 그제서야 조건 문구 공개.
2. **뒤늦은 발견**: 배치(09·13·17·21시)로 발견된 뱃지는 다음 접속 때 큐로 순차 연출 — "어제 딴 뱃지가 있어요".
3. 뱃지함에는 획득분만 컬러로, 미획득분은 `???`로 개수만. 조건은 끝까지 비공개.
4. `신기록`(클래스5 게임 최고점)은 뱃지가 아니라 **기록 갱신 알림**으로 반복 노출 — 뱃지 인플레 방지.

### 5.7 획득 즉시 공유 (자랑)
1. 획득 순간 `/learn`에 **뱃지 획득 모달** — 뱃지 아트 + 획득 조건 + `자랑하기` 버튼
2. `자랑하기` → 클라이언트 canvas로 **공유 카드 PNG** 생성 (뱃지 아트 · 표시명 · 레벨/칭호 · 획득일 · plantor 로고)
3. 공유 경로:
   - `navigator.share({ files })` 지원 시 → 카톡/인스타 등 네이티브 공유 (모바일 기본 경로)
   - 미지원 시 → PNG 다운로드 + 링크 복사 폴백
4. 공유 링크: `/b/[badgeCode]?n=표시명` — **정적 export 제약상 뱃지 종류별 정적 페이지를 사전 생성**하고, OG 이미지는 뱃지별 고정 PNG(`public/badges/og/*.png`)를 사용. 이름은 쿼리스트링으로 화면에만 표시.
5. ~~**개인정보** — 공유 카드에는 **닉네임만**~~ → **2026-08-05 폐기.** 피드·공유 카드 모두 학년+실명을 쓴다(사용자 확정).
   닉네임 기능(`setRewardNickname`·`children.displayName`)은 UI가 끝내 만들어지지 않아 제거했다. 옛 정책: 실명은 어떤 경우에도 카드·공유 링크에 넣지 않는다. 닉네임 최초 설정 UI가 필요하며, 미설정 시 기본값은 학생 이름이 아니라 `새싹123` 형태의 자동 생성 닉네임

## 6. 데이터 모델 (Firestore)

```
children/{childId}
  xpTotal, level, points, pointsSpent, streak, bestStreak, lastStreakDate, displayName
  equipped { base, hair, outfit, hat, prop, background, frame, effect }   // 착용 중인 아바타

children/{childId}/xpLedger/{date}_{serviceSlug}      ← 멱등 키
  xp, points, quality, qualityRaw,
  breakdown { base, quality, volume, streakMult, lateFactor }, computedAt

children/{childId}/badges/{badgeCode}
  earnedAt, context{...}, shareCount, seen        // seen=false면 다음 접속 때 연출 큐로

children/{childId}/stats/summary                   // 히든 조건 판정용 누적 상태
  testScoreStreak, conceptPerfectStreak, vocaGoodStreak,
  gameHighScore { grammar, book, movie },          // '신기록' 판정
  weeklyQuality { serviceSlug: [지난주Q, 이번주Q] } // '껑충' 판정
  unitTotal, allClearStreak, lastEarlyBird...

children/{childId}/inventory/{itemId}              { acquiredAt, source: "point"|"level"|"badge" }

shopItems/{itemId}
  name, category, cost, rarity, assetUrl, active,
  unlock { type: "point" | "level" | "badge", level?, badgeCode? }
```

구매는 트랜잭션으로 `points 차감 + inventory 추가`를 원자적으로 처리한다(이중 구매 방지). 실물 배송이 없으므로 `purchases` 승인 큐는 두지 않는다.

**멱등성이 이 설계의 생명줄**: 배치가 하루 4번(09·13·17·21시) 돌고 클릭 인증도 별도로 돌기 때문에, 같은 날짜·서비스가 여러 번 계산된다.
→ `awardXp`는 항상 **그 날짜·서비스의 XP를 처음부터 재계산**하고, **원장에 저장된 이전 값과의 차액만** `children` 합계에 반영한다. (누적 덧셈 금지)

## 7. 구현 순서

| # | 작업 | 상태 | 검증 결과 |
|---|---|---|---|
| 1 | **스크래퍼 품질 지표 확장** — 오토보카 활동별 1회정답률·회차별 테스트·활동별 포인트·오답소탕, 매일국어 단계별 xp·`xpGot/xpMax`·어휘 세트 등급, 클래스5 `activity_list` 요약(카드 1회정답률·`total_duration`·게임점수), 클래스카드 `startHour` | **[완료]** | 라이브 스크래핑으로 전 필드 수집 확인 (스펠 88/38/100%, 단계별 xp, 카드정답률 100/93/57%, 게임 1,690점, voca 등급 `good`) |
| 2 | **XP 엔진** `functions/src/rewards.ts` — 순수함수 `computeXp()` + 멱등 `awardRewards()`. `writeAutoLog` 직후 호출(클릭·배치 공통) | **[완료]** | 실데이터 11건: 서비스 1건당 min 60 · 중앙 97 · max 100 XP. 실력 차가 XP 차로 이어짐(스펠 38%→60XP vs 100%→100XP) |
| 3 | **레벨/포인트 UI** — `/learn` 최상단 위젯(아바타·칭호·레벨 바·연속·포인트·뱃지) | **[완료]** | 타입체크·lint·정적 빌드 통과. 라이브 확인은 배포 후 |
| 4 | **뱃지 엔진(히든 46종) + 발견 연출 + 공유 카드** — 누적 상태(`stats/summary`)로 연속·최고기록·주간 품질 추적 | **[완료]** | CSS 애니메이션(0.6초 뒤 카드 뒤집힘), 공유는 `navigator.share(files)` → 실패 시 PNG 다운로드 |
| 5 | **아바타 + 상점** — 28개 아이템 8슬롯, 인벤토리/착용, 포인트 구매(트랜잭션), 레벨·뱃지 해금 | **[완료]** | 구매·착용·닉네임·뱃지확인 콜러블 4종. 아트는 이모지 폴백 → 에테르 API 픽셀아트로 교체 예정 |
| 6 | **소급 적립 (1회성 스크립트)** `scripts/retro-rewards.js` — 최근 30일 완료분만, Lv10·1,000P 상한, 뱃지 제외 | **[완료·미실행]** | `--dry-run` 지원. 배포 후 실행 |

### 남은 일
- **배포** — functions(리워드 엔진·API) + hosting(위젯) + firestore.rules. 배포 전까지 운영에는 아무 변화가 없다
- 소급 스크립트 실행 (`node scripts/retro-rewards.js --dry-run` 먼저)
- 아바타 파츠 픽셀아트 생성(에테르 API) → `/public/avatar/{slot}/{id}.png`

## 8. 결정 사항

### 확정됨
- ~~인증샷(self) 서비스 XP~~ → **지급하지 않음.** XP·포인트·뱃지는 **자동인증 4종(오토보카·클래스카드·매일국어·클래스5) 전용**
- ~~실물 보상~~ → **없음.** 상점은 플랜토 내부 디지털 아이템(아바타·스킨)만, 전액 자동 처리
- 뱃지는 **히든 방식** (성실 4종만 공개)

- **매일국어 속도 페널티** → **유예 없이 즉시 적용** (§2.1)
- **공유 카드 표시명** → **닉네임만** 사용, 실명 미노출 (§5.7)
- **소급 적립** → **최근 30일치를 쳐주되 상한을 둔다** (아래)

### 소급 적립 규칙 (확정)
- 대상: 기존 `learningLogs` 최근 **30일**, 자동인증 4종
- 계산: 과거 로그에는 품질 필드가 없으므로 **완료점(60)만** 적용. 품질·연속·분량 보너스는 소급하지 않음
- **상한: 최대 Lv 10 / 1,000P** — 초기 격차가 과하게 벌어지는 걸 막는다
- 뱃지는 소급하지 않음 (§9 — 판정에 필요한 필드가 과거 로그에 없음)
- 학생 화면에는 `그동안 학습한 기록으로 Lv10 · 1,000P를 받았어요` 형태의 오픈 보너스로 1회 안내

## 9. 리스크

- 클래스5 `activity_list`는 **학생·날짜당 1요청 추가** — 완료 학생만 조회해도 배치 4회 × 학생 10명 ≈ 40요청/일 (감당 가능)
- 클래스5 카드 1회정답률에는 **더빙/게임 등 채점 없는 활동(`is_unscored_activity`)이 섞여 있어** 실측 중앙 70%가 낮게 잡혔을 수 있음 → 2단계에서 해당 활동 제외 후 **앵커 재보정 필요**
- 클래스카드 어휘/본문·듣기는 표본이 사실상 없어 Q 고정 0.5로 시작, 데이터가 쌓이면 조정
- 자동인증 스크래핑이 실패하면 XP가 안 쌓임 → 실패 시 재시도/알림 필요 (기존 폴백 UI와 연결)
- **히든 뱃지 소급 판정 불가**: 대부분의 조건이 지금은 저장하지 않는 필드(단어별 `try_cnt`, 단계별 xp, `activity_list` 등)를 쓴다. 과거 로그에는 그 값이 없으므로 뱃지는 **1단계 배포 시점부터** 쌓인다. 학생에게는 "지금부터 모으는 중"으로 안내
- **아바타 아트 에셋 제작이 별도 작업**: 레이어 합성(베이스/헤어/의상/모자/소품/배경/프레임)을 하려면 파츠가 규격이 맞아야 한다. SVG 자체 제작 또는 무료 라이선스 소싱이 5단계의 실질 병목 — 최소 세트(베이스 2 · 의상 6 · 액세서리 6 · 배경 4 · 프레임 4)로 시작 권장
