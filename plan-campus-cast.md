# 기획 — 캠퍼스 등장인물·동물 확충

> **상태: Phase 1 완료(2026-08-13) · Phase 2 는 A-2(반려동물)로 이미 나감.**
> 에셋은 **Kenney 원툴**로 간다 — 다른 벤더는 검토에서 뺀다.

근거는 `research-campus-cast.md`.

목표를 한 줄로: **캠퍼스에 사는 것의 종류를 늘린다.**
캐릭터 12 → 21종, 그리고 마을에 동물을 들인다.

---

## Phase 1 — 캐릭터 9종 [완료 2026-08-13]

mini-\* 팩 9종은 지금 쓰는 12종과 **뼈 7개 이름이 동일**하다. 클립을 새로 뽑지 않는다.

> ### 하면서 드러난 것 (계획서에 없던 것)
>
> 1. **팩마다 colormap.png 가 다르다.** 색 세트는 거의 같은데 자리가 다르다
>    (mini-arcade·mini-market 만 mini-characters 와 완전히 동일). 런타임은 팔레트
>    한 장을 전 캐릭터에 갈아 끼우므로 자리가 다르면 색이 통째로 어긋난다 —
>    굽는 단계에서 **UV 를 기준 팔레트 자리로 옮긴다**(`family_map`).
>    · 옮길 자리는 원래 16색(아래 두 줄)만 쓴다. 윗줄은 런타임이 찍는 살색 자리다.
>    · 오차가 작은 것부터 배정하고 찬 자리는 건너뛴다 — 두 색이 한 자리로 겹치면
>      다른 부위가 같은 색이 된다.
>    · 텍스처 교체는 `filepath` + `reload()` 로 안 된다(임포터가 pack 해 둔다).
>      머티리얼의 TEX_IMAGE 노드를 직접 갈아 끼워야 한다.
> 2. **서버에도 목록이 있다** — `functions/src/campus-shop.ts` 의 `MODELS`.
>    여기 없는 id 는 구매가 "없는 물건"으로 막혀 **영영 잠긴 칸**이 된다.
> 3. **부위 자동 판정이 둘 틀렸다**(예외 표로 덮음). archer 는 장갑이 살색 계열에
>    있어 손 대신 장갑을 살로 잡았고(얼굴이 머리카락으로 밀림), orc 는 초록
>    얼굴·어깨가 팔뚝과 다른 계열이라 갈라져 있었다.
> 4. **투구가 안경으로 잡혔다**(soldier). 폭만 보던 판정에 "세로로 납작할 것"을
>    더했다 — 안경 0.046, 투구 0.443.
> 5. hero 는 머리카락이 얼굴과 같은 계열이라 **'머리' 슬롯이 안 먹는다**(male-b
>    처럼 대머리로 읽힌다). male-f 처럼 하의가 없는 것(soldier)도 선례가 있다.
> 6. 용량은 예상 +360KB 가 아니라 **+583KB**(캐릭터 1.26MB → 1.85MB).

| 출력 이름 | 원본 | 비고 |
|---|---|---|
| `gamer` | mini-arcade / character-gamer | 헤드셋 쓴 학생 |
| `arcade-clerk` | mini-arcade / character-employee | 앞치마 |
| `market-clerk` | mini-market / character-employee | ⚠ 위와 원본 파일명이 같다 |
| `skate-boy` | mini-skate / character-skate-boy | |
| `skate-girl` | mini-skate / character-skate-girl | |
| `archer` | mini-forest / character-archer | 후드 |
| `hero` | mini-dungeon / character-human | 모험가 |
| `orc` | mini-dungeon / character-orc | 초록 피부 — 빼려면 배열 한 줄 |
| `soldier` | mini-arena / character-soldier | 군인 — **빼는 쪽을 권합니다** |

### 작업

1. **`scripts/campus-prep-kenney.py` 를 리스트 방식으로 바꾼다** → 검증: 기존 12종을
   다시 구워 바이트 크기가 이전과 같은지 확인
   - 지금은 폴더 하나를 글롭하고 출력 이름이 `character-` 만 뗀다. 팩이 여러 개가
     되면 **`character-employee` 가 충돌한다.**
   - 형제 스크립트 `campus-prep-kit.py` 가 이미 `"소스 => 출력이름"` 리스트를 쓴다.
     같은 규약으로 맞춘다. 새 개념을 만들지 않는다.
   - `CLIP_SOURCE='character-male-a'` 는 그대로 — 클립은 계속 male-a 한 종에만 둔다.

2. **9종을 굽는다** → 검증: 개당 40KB 안팎, 애니메이션 0개 (`export_animations=False` 경로)

3. **`scripts/campus-part-cells.py` 재실행** → 검증: `part-cells.json` 의 `models` 키가
   12 → 21개, 각 모델에 `skin/hair/top/bottom` 이 **모두** 잡혔는지 확인
   - 이 스크립트는 출력 폴더를 글롭하므로 인자 변경이 없다.
   - ⚠ 부위 판정이 휴리스틱이다(머리 뼈 + 뒤통수 z<0 = 머리카락). archer 후드,
     gamer 헤드셋처럼 **처음 보는 형태는 오분류될 수 있다.**
     한 부위라도 비면 그 모델은 목록에서 뺀다 — 색이 안 바뀌는 캐릭터를 섞지 않는다.

4. **`avatar-kenney.js` 의 `MODELS` 에 9개 추가** → 검증: 꾸미기 패널 머리 슬롯이
   21칸(+민머리)으로 늘고 썸네일이 전부 뜨는지
   - `CREDIT` 문자열도 고친다 — 지금은 "Mini Characters" 한 팩만 적혀 있는데
     출처가 6팩이 된다. CC0 라 의무는 아니지만 적어 둔 걸 틀리게 두지 않는다.
   - 저장된 `look.model` 은 건드리지 않는다. 추가만 하므로 마이그레이션이 없다.

5. **꾸미기 패널 레이아웃 확인** → 검증: 배포 후 라이브(`plantor.web.app/campus`)에서
   모바일·데스크탑 각각
   - ⚠ 최근 커밋(c4db368)에서 "잘린 색 줄과 빈 오른쪽"을 잡았다.
     칸이 12 → 22개로 늘면 **그 수정이 되돌아갈 수 있다.** 여기가 이번 작업의
     제일 큰 리스크다. 로컬 서버를 안 쓰므로 배포 후 확인이 유일한 검증이다.

**용량**: 약 +360KB (현재 캐릭터 1.2MB → 1.6MB)

---

## Phase 2 — 동물 24종 (cube-pets) [완료 — A-2 로 나감]

> 아래 표에서 **A-2(반려동물)** 로 갔다. `pets.js` · 캐릭터 창의 '펫' 탭 ·
> `look.pet` 에 저장되고 플레이어를 따라다닌다. A-1(장식)은 안 했다.

여기부터는 **결정이 필요합니다.** 아래 셋 중 어느 것이냐에 따라 작업량이 크게 갈립니다.

### 결정 A — 동물을 어떻게 쓸 것인가

| 안 | 내용 | 작업량 | 비고 |
|---|---|---|---|
| **A-1 장식** | 방·마을에 놓는 오브젝트. 제자리에서 idle 재생 | 작다 | 기존 `decor.js` 흐름에 얹힌다. 상점에서 산다 |
| **A-2 반려동물** | 1마리를 골라 플레이어를 따라다닌다 | 중간 | 추적·경로·walk/run 전환이 새로 필요 |
| **A-3 야생** | 마을을 자유롭게 배회, 말 걸면 반응 | 크다 | 네트워크 동기화까지 얽힌다 |

제 추천은 **A-1 → A-2 순서**입니다. A-1 은 이미 있는 배치·상점·인벤토리를 그대로
쓰므로 새 개념이 `pets.js` 로더 하나뿐이고, 그게 A-2 의 준비물이기도 합니다.
A-3 은 게스트 실시간 문제([[campus-guest-no-realtime]])와 얽혀 지금 열 판이 아닙니다.

### 공통 작업 (어느 안이든 필요)

1. **`scripts/campus-prep-kit.py` 에 애니메이션 보존 옵션을 판다** → 검증: 구운 GLB에
   클립 8개가 남아 있는지
   - 지금 `export_animations=False` 로 **박혀 있다.** 동물은 애니메이션이 본체다.
2. **`public/campus/lib/pets.js`** — 로더 → 검증: 24종 로드, idle 재생, 색 정상
   - 스킨이 없다. `SkeletonUtils.clone` 이 아니라 일반 `clone(true)` 로 복제한다.
   - 노드 이름(`body`, `leg-front-left`...)에 트랙이 걸려 있어 복제본마다
     `AnimationMixer` 를 따로 물려야 한다.
   - 팔레트 텍스처라 **NearestFilter 필수**, `metalness = 0` (기존 함정 2개 그대로).
3. **용량 실측 후 종수 결정** → 검증: 3종만 먼저 구워 개당 크기를 잰다
   - 원본 24개가 3.2MB인데 텍스처를 파일마다 embed 하면 **더 커진다.**
     기준을 넘으면 종수를 줄이거나 colormap 을 공유 텍스처로 빼는 길을 판다.

### A-1 을 고를 경우 추가

4. `items.js` 에 동물 항목 + 가격, `decor.js` 에 배치 항목 → 검증: 사서 놓고 저장 후
   새로고침했을 때 그대로 있는지
   - `sanitizePlace` 가 `DECOR_BY_ID` 로 걸러내므로 등록 전에는 저장이 씹힌다.
   - 가격 티어를 어디에 둘지 결정 필요 (지금 최고가가 소파 800P)

---

## Phase 3 — 보류

- **platformer-kit oo\* 5종** — head 뼈가 없어 클립이 일부 죽는다. 마스코트로 쓸
  자리가 정해지면 그때.

---

## 안 하는 것

- blocky-characters / graveyard-kit / prototype-kit 도입 — 아트 톤이 깬다
- **다른 벤더 도입 — Kenney 원툴로 확정.** 캐릭터 본체는 물론 착용물도 마찬가지다.
  리그(뼈 7개 이름)와 팔레트(512×512 colormap 한 장)가 맞아떨어져서 지금 구조가
  성립한다. 벤더가 섞이면 클립 리타게팅과 색 커스텀을 계열마다 이중 관리하게 된다.
- 기존 12종·40개 소품 건드리기 — 하게 되더라도 전부 **추가만** 한다
