# 기획 — 캠퍼스 밤 조명 (가로등)

> 상태: **완료 · 라이브** (2026-08-13). 아이디어 4개 중 1번.
> 나머지(광장 · 매점 진열 · 계절)는 이 문서 뒤에 붙이지 않고 각각 따로 잡는다.
>
> ### 만들면서 드러난 것 두 가지 (계획서에 없던 것)
>
> 1. **등갓 렌즈의 법선이 (0, -1, 0) 이다** — 정확히 아래를 본다. 부감 28° 카메라
>    에서는 **영영 안 보이는 면**이라, 자체발광을 아무리 올려도 켜진 티가 안 나고
>    바닥에만 빛이 고여 '광원 없는 웅덩이'가 됐다. 갓 자리에 카메라를 향하는
>    스프라이트(`lamp-halo`)를 하나 세워 해결했다. 크기는 라이브에서 눈으로 맞췄다
>    (갓 높이의 0.30배 — 0.42배는 지붕까지 먹었다).
> 2. **야외 바닥이 밤에 안 어두워졌다.** `plate()` 주석은 "빛을 받는 재질"이라고
>    적혀 있었지만 자체발광이 0.9 라, 낮 `0.9c + 1.0c` · 밤 `0.9c + 0.6c` 가 둘 다
>    흰색으로 잘려 차이가 0 이었다(형광 잔디). **발광을 밤에 눕히지 않으면 등을
>    켜도 아무것도 안 얹힌다** — 가로등이 성립하려면 이게 먼저였다.
>    `groundLit` 에 재질과 원래 색을 들고 있다가 `0.9 - night*0.66` 으로 내린다.

## 왜

별 2200개 · 달 위상 · 먼동까지 만들어 놨는데 **지면은 깜깜하다.** 밤이
"어두운 낮"으로 보이는 이유가 이것이다. 지금 밤에 하는 일은 `sun.intensity`·
`hemi.intensity` 를 낮추는 것뿐이라([map.js:1815](public/campus/lib/map.js)),
어두워질 뿐 **불이 켜지는 곳이 없다.**

## 무엇을

`city-kit-roads` 의 가로등 3종(굽은 등 · 사각 등 · 쌍등). 밤이 되면 등갓이
켜지고 **바닥에 빛 웅덩이**가 깔린다.

**실제 광원(PointLight)은 안 쓴다.** three.js 는 광원이 늘 때마다 셰이더를
다시 굽고, 등을 스무 개 놓으면 그대로 프레임이 죽는다. 등갓은 emissive 로,
바닥은 반투명 원판(데칼)으로 흉내 낸다 — 비용이 드로우콜 한 장이다.

## 걸림돌과 답

**등 전체가 재질 하나다.** GLB 를 뜯어 보니 mesh 1 · material 1(`colormap`)
이라, 그대로는 기둥까지 같이 빛난다. 그런데 **렌즈만 주황색**이다
(uv 16,320 `#f1976c`, 정점 6개 — 나머지는 전부 무채색 회백). 그래서 굽는
단계에서 **채도로 갈라** 렌즈 면만 `lamp-glow` 재질로 떼어낸다. 셀 좌표를
박아 넣지 않고 채도로 판단하므로 등 4종에 그대로 통한다.

## 작업

1. **`campus-prep-kit.py` 에 `KIT_GLOW=1` 을 판다** → 검증: 구운 GLB 의
   재질이 2개(`colormap` · `lamp-glow`)인지, 렌즈 정점 수가 원본과 같은지
   - 면의 UV 중심에서 텍스처를 찍어 **채도 > 0.25 면 `lamp-glow`** 로 보낸다.
   - `KIT_ANIM` 이 이미 같은 방식의 스위치라 새 개념이 아니다.

2. **등 3종을 굽는다** → 검증: `kit/` 에 3개, 개당 10KB 미만

3. **`decor.js` 에 항목 추가** → 검증: 꾸미기 팔레트 '야외' 에 세 칸이
   뜨고 썸네일이 나오는지
   - `s` 는 구운 모델을 재서 **높이 3.2m** 가 되게 잡는다(캐릭터 1.3m 의 2.5배).

4. **빛 웅덩이** → 검증: 등을 놓고 자정에 바닥이 밝아지는지
   - `buildDecor` 에서 `lamp-glow` 재질을 가진 모델이면 **등갓 아래**에 원판을
     하나 붙인다(등갓 위치는 그 재질을 쓰는 메시의 바운딩 중심 — 굽은 등은
     기둥에서 옆으로 비켜 있어 모델 중심으로 잡으면 어긋난다).
   - 방사형 그라디언트 캔버스 한 장을 모듈 전역으로 공유. `depthWrite:false`,
     `AdditiveBlending`.

5. **밤에 켠다** → 검증: 낮에는 안 빛나고, 19시부터 서서히 밝아져 자정에 최대
   - `drawDecor` 가 재질을 모을 때 이름으로 갈라 담고, 렌더 루프의 하늘 갱신
     옆에서 `s.night` 로 `emissiveIntensity` 와 원판 `opacity` 를 움직인다.
   - 실내에서도 돈다(`sky.update` 는 실내에서도 값을 돌려준다).

## 안 하는 것

- **도로 타일** — 등만 뽑는다. 바닥 타일 21장을 지운 결정을 되돌리지 않는다.
- **건물 창문 불 켜기** — 창은 벽 텍스처의 일부라 갈라내려면 건물 21종을 전부
  다시 봐야 한다. 등이 먼저다.
- 등을 **공용 공간에 미리 깔아 두는 것** — 배치는 운영자(사용자)가 한다.
  나는 놓을 수 있게만 만든다.
