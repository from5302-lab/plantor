# 캠퍼스 캐릭터 색 커스텀 — research

작성: 2026-08-11
방법: GLB 12개를 직접 뜯어 정점의 (UV칸 · 뼈 · 높이 · 소속 메시)를 교차했다.
스크립트는 스크래치패드에 있다(`part_map2.py`). 텍스처를 눈으로 보고 찍지 않았다 —
어느 칸이 상의고 어느 칸이 소매인지는 그림만 봐서는 알 수 없다.

## 1. 에셋 구조 (확인 완료)

- 캐릭터 하나 = 메시 **둘**(`body-mesh`, `head-mesh`) + 뼈 **일곱**
  (root · torso · head · arm-left/right · leg-left/right). 12종이 전부 같다.
- 색은 **512×512 팔레트 텍스처 한 장**(`colormap`)을 UV 로 찍는다.
  샘플러 `magFilter 9728` = **NEAREST**(선형이면 인접 칸이 섞인다).
- 팔레트는 **8×8 = 64칸**, 칸 하나가 64px. 메모리에 적어 둔 "색칸 64개"가 맞다.
- ⚠ **칸은 단색이 아니다.** 칸 안이 세로 그라데이션이고(텍스처 전체 고유색 255개),
  메시가 칸 안의 높이를 달리 찍어 음영을 낸다. 그래서 칸을 단색으로 덮으면
  캐릭터가 평평해진다 — **명암 램프를 유지한 채 색조만 옮겨야 한다.**
- 모델 하나가 실제로 쓰는 칸은 **6~10개**뿐이다. 64칸 중 50칸 넘게 비어 있다.

## 2. 원안이 안 되는 이유 (핵심 발견)

**칸은 부위가 아니라 색 단위다.** 같은 색이면 부위가 달라도 한 칸을 나눠 쓴다.
실제로 확인된 것:

| 모델 | 칸 | 나눠 쓰는 부위 |
|---|---|---|
| `male-b` | (5,7) | 머리카락 + 신발 (둘 다 갈색) |
| `female-e` | (0,6) | 머리카락 + 하의 (둘 다 검정) |
| `female-c` | (1,6) | 머리카락 + 하의 (둘 다 회색) |
| `male-c` | (0,6)(0,7) | 몸통 옷 + 이목구비 |

→ **"칸을 칠한다"는 원안을 그대로 쓰면 머리색을 바꿨을 때 신발색도 같이 바뀐다.**

## 3. 대안 — UV 재배치

빈 칸이 50개 넘게 남아 있으므로, 칠하기 전에 **부위를 자기 칸으로 이사시킨다.**

1. 부위의 원본 칸에서 64×64 를 떠서 **빈 칸에 복사**(명암 램프 그대로)
2. 복사본을 목표 색으로 **색조 이동**(HSL 의 H·S 만 옮기고 L 은 보존)
3. 그 부위 정점의 **UV 만 새 칸으로 평행이동**

이러면 공유가 풀려 부위마다 독립적으로 색이 바뀐다.

**⚠ 복제 함정 두 개** — 지금 코드가 `SkeletonUtils.clone` 을 쓴다
([avatar-kenney.js:17](public/campus/lib/avatar-kenney.js:17)).
- clone 은 **geometry 를 공유한다.** UV 를 그 자리에서 고치면 그 모델을 쓰는
  **모든 캐릭터**(다른 접속자 포함)의 UV 가 같이 바뀐다 → 사람마다 geometry 복제 필요
- **material·texture 도 공유한다** → 사람마다 캔버스 텍스처를 따로 물려야 한다

## 4. 부위 분류 — 자동으로 어디까지 되나

단위를 칸이 아니라 **(메시, 칸) 정점 그룹**으로 잡고, 색이 아니라 구조로 판정했다.

| 부위 | 판정 근거 | 신뢰도 |
|---|---|---|
| 살 | head-mesh 와 **팔 뼈** body 정점이 함께 쓰는 칸(얼굴+손) | 높음 |
| 하의 / 신발 | 다리 뼈 전용. 높이 0.12 로 가름 | 높음 |
| 상의 | 몸통 + 팔 뼈 | 중간 |
| 머리카락 | head-mesh 전용 그룹 중 가장 크고 높은 것 | **낮음** |

머리·다리를 함께 쓰는 칸을 살로 오인하지 않게 고쳤더니(검정 머리 + 검정 신발)
대부분 맞지만, 12종 중 **최소 3종이 눈에 띄게 틀린다**:
- `female-e` 는 살 그룹을 아예 못 찾는다
- `male-b` 의 "몸통옷"(5,6)(5,7)은 살구색이라 목·살일 가능성이 크다
- `female-a` 의 살 대표색이 `#b06041` — 어두운 피부일 수도, 오분류일 수도 있다

**그림을 보지 않고는 확정할 수 없다.** 자동 분류를 그대로 믿고 UI 를 얹으면
"머리색"을 눌렀는데 신발이 바뀌는 캐릭터가 12종 중 서넛 섞인다.

## 5. 붙는 자리

- 저장: `saveCharacter(look, body)` 가 이미 있다([store.js:76](public/campus/lib/store.js:76)).
  `look.colors = {skin, hair, top, bottom}` 를 얹으면 그대로 저장된다
- 전파: `net.updateMeta(look, body)` 가 다른 접속자에게 이미 look 을 보낸다
- UI: 캐릭터 패널이 이미 있다([map.js:1320](public/campus/lib/map.js:1320)) — 12종 그리드 아래에 색 줄을 붙이면 된다
- 게스트: 캐릭터 패널 자체가 로그인 전용이다(`dressBtn.hidden = !ME`)
