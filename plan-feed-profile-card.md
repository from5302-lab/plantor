# 계획: 피드 맨 위 프로필 카드

작성: 2026-08-06 · 상태: **검토 대기 (승인 전 구현 금지)**

## 사용자 확정 사항
1. 상단 탭(SNS 골격)은 **다음 단계**. 이번엔 프로필 카드만
2. 카드 기능은 **`/learn` 헤더와 동일** (아바타→상점, 뱃지→뱃지판)
3. **학부모는 자녀 카드를 나란히**, 운영자는 카드 없음
4. 학생 홈(`/learn`)은 그대로 둔다

---

## 1. 조사 결과 — 설계를 가르는 두 가지

### (가) 재료는 이미 다 있다
`RewardHeader`(106줄)가 사실상 프로필 카드다. 아바타·칭호·레벨·연속·XP 진행바·뱃지 수·포인트 상점까지
전부 들어 있고, `useRewards(childId)` 하나로 `children/{childId}` + `stats/summary` + `badges` + `inventory`를
실시간 구독한다. **새로 만들 게 아니라 childId만 풀어서 얹으면 된다.**

### (나) 학부모는 자녀 카드를 **읽을 수는 있지만 조작할 수 없다**
- 읽기: `firestore.rules:104` — `children/{childId}/{sub=**}` 를 **가족이면 읽을 수 있다**. 뱃지·인벤토리·연속 전부 보인다. ✅
- 쓰기: 상점 구매·아바타 착용은 콜러블이고, `rewards-api.ts:12` 의 `resolveChild()` 가
  **호출자 본인의 `plantor_id` 로 학생을 찾는다**. 학부모가 누르면 "학생 정보를 찾을 수 없습니다" 로 실패한다. ❌

→ **학부모가 보는 자녀 카드는 읽기 전용이어야 한다.** 아바타·뱃지 버튼을 그대로 두면
누를 때마다 에러가 난다. 뱃지판은 읽기만 하므로 열어도 되지만, 상점은 막아야 한다.

이것 때문에 확정사항 2번("/learn 헤더와 동일")은 **학생 본인 카드에만** 적용한다.
학부모 카드는 같은 모양, 조작만 없음. 다르게 하려면 알려달라.

---

## 2. 구현

| # | 작업 | 검증 |
|---|---|---|
| 2-1 | `useMyChildIds(uid, role)` 훅 신설 — 학생은 본인 childId 1개, 학부모는 가족 자녀 전체 | 학생 1개·학부모 n개 반환 |
| 2-2 | `RewardHeader` 에 `readOnly` 를 **실제 동작까지** 확장 — 지금은 뱃지 발견 연출만 막는다. 상점 진입(아바타 클릭·상점 버튼)을 잠근다 | 학부모 카드에서 상점 안 열림 |
| 2-3 | `FeedProfileCard` — 카드 + 이름/학년 줄. 학생은 그대로, 학부모는 자녀 이름 표기 | — |
| 2-4 | `FeedList` 상단에 삽입. 로그아웃·운영자는 없음 | 역할별 노출 확인 |
| 2-5 | `/community` 와 `/` 양쪽에 동일 적용 (같은 `CommunityShell`) | 두 경로 동일 |

### childId 해석 (2-1)
`useChildData`(241줄)는 구독·로그·과제까지 들고 와서 피드에 얹기엔 무겁다. 해석 부분만 뽑아 가벼운 훅으로 만든다.
경로는 기존과 **동일한 규칙**을 따른다 — 규칙상 `children` 은 `familyId` 로 좁혀야만 조회된다
(`firestore.rules:94` 주석, 2026-08-05 장애 이력).

- 학생: `users/{uid}.plantor_id` + `.familyId` → `children where familyId == myFamilyId` → `loginId` 일치 문서
- 학부모: `families where userId == uid` (없으면 `users/{uid}.familyId`) → 같은 쿼리로 자녀 전체

`useMyFamilyNames` 가 이미 같은 해석을 하고 있다. 중복을 만들지 말고 **그 훅을 childId까지 반환하도록 넓힌다.**

### 배치와 모양 (2-3, 2-4)
- 피드 최상단, "오늘 요약" **위**. 내 것이 먼저고 남의 기록이 그다음이다.
- 자녀가 여럿이면 세로로 쌓는다. 가로 스크롤은 카드가 잘려 보여 학부모가 둘째를 놓친다.
- 카드 폭은 피드 본문(`max-w-[600px]`)에 맞춘다.

---

## 3. 하지 않을 것
- 상단 탭·프로필 페이지 — 다음 단계
- `/learn` 손대기 — 확정사항 4
- 학부모용 구매 권한 열기 — 리워드는 학생이 직접 모아 쓰는 것이라는 설계 전제를 건드린다

---

## 4. 검증
1. `npm run build` · 손댄 파일 lint 0
2. 로그아웃 — 카드 없음, 기존 피드 그대로
3. 학생 — 카드 1장, 아바타·뱃지·상점 동작
4. 학부모 — 자녀 수만큼 카드, 상점 잠김(뱃지판은 열림)
5. 운영자 — 카드 없음
6. 배포 후 라이브 확인

**3·4·5는 내가 로그인할 수 없어 직접 못 본다.** 배포 후 실제 계정으로 봐줘야 한다.

---

## 5. 다음 단계에 남길 메모 (SNS 골격)
지금 카드를 `FeedList` 안에 박지 않고 **`CommunityShell` 이 조립**하게 두면,
나중에 상단 탭이 생겼을 때 카드를 프로필 탭으로 옮기기가 쉬워진다. 그렇게 짠다.
