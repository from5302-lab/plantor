# 가계부 "이번 달 정산" — 받을 돈(미입금) / 받은 돈(입금완료) 2단 구성

## 배경 (사용자 확인 완료)
- 사장님은 입금받을 때 **회원탭 "입금확인" 버튼**을 사용함.
- 입금확인 = 만료일 연장 + 가계부 수입 자동기록(`paymentKey`) + 알림톡 → 한 번에 처리됨.
- 문제: 입금확인하면 그 사람이 가계부 "들어올 예정" 목록에서 **그냥 사라짐** → "이번 달에 누가 냈는지" 흔적이 없음. 미입금자 관리가 안 됨.
- 기존 수동 토글은 `expect_` 키, 입금확인 자동기록은 `paymentKey` 키 → 서로 안 맞아 `0/19`가 안 움직임(혼란).

## 목표
가계부 패널을 **두 칸**으로:
- **받을 돈 (미입금)**: 이번 달 만료(=받아야 할) 구독/수업/AI 중 아직 입금확인 안 된 사람 → 미입금자 챙김.
- **받은 돈 (입금완료)**: 이번 달 입금확인으로 자동 기록된 수입 → 누가 냈는지 확인.

사장님 동선: **회원탭 입금확인만** 누르면 자동으로 "받을 돈"에서 빠지고 "받은 돈"으로 ✅ 이동.

## 데이터 매핑 (기존 데이터 그대로 사용 — 신규 인프라 X)
- 받을 돈 = `useAdminBilling(month).items` (구독 endDate가 이번 달). 입금확인 시 연장돼 자동 제외됨.
- 받은 돈 = `entries` 중 이번 달 + `paymentKey`가 `renewal_*` 또는 `direct_*` 인 수입.
  - renewal 기록: memo `{parentName} 구독연장`, amount=합계.
  - direct 기록: memo `{studentNames} 수업료`, amount=수업료.
  - 표시는 memo + amount 그대로.

## 변경 작업
1. `src/components/vault/vault-shell.tsx` — `VaultEntry` 타입에 `paymentKey?` 추가. ✅(완료)
2. `src/components/vault/expected-income-panel.tsx` — 전면 재구성:
   - 수동 토글/`expect_` 로직 제거.
   - 헤더: "이번 달 정산 · 받을 돈 ₩X / 받은 돈 ₩Y".
   - 펼치면 **받을 돈(미입금)** 리스트 + **받은 돈(입금완료)** 리스트 두 섹션.
   - 받을 돈 0건 + 받은 돈 0건이면 패널 숨김.

## 검증
- `npm run build` 통과.
- 입금확인 → 받을 돈에서 빠지고 받은 돈에 등장(데이터 흐름상 자동).
- 미입금자가 받을 돈에 남아 한눈에 보임.

## 비고
- 과거 수동 토글(`expect_`) 데이터는 현재 0건(0/19)이라 제거해도 영향 없음.
- AI 패키지 단독 입금확인 등 일부 경로는 `paymentKey` 미기록일 수 있음 → 우선 renewal/direct만 집계, 누락 보이면 추후 보완.
