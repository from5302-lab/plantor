# 리서치 — 캠퍼스에 높이 붙이기 (곡면 제거 + 점프/등반)

**질문**: 곡면을 없애고, 어디서나 점프로 올라 다닐 수 있게 만들 수 있나?
**답**: 가능하다. 손댈 곳이 좁고, 대부분 기계적이다. 다만 **잃는 것**이 있다.

---

## 1. 지금 상태 (실측)

### 점프는 그림뿐이다
```js
// map.js — 점프
const JUMP_DUR = 0.5, JUMP_H = 0.7;
const hop = jumpT > 0 ? Math.sin((1 - jumpT/JUMP_DUR) * Math.PI) * JUMP_H : 0;
player.root.position.y = hop;      // ← 렌더 위치만 바꾼다
```
`P` 에는 `y` 가 없다(`{x, z, yaw, walkT}`). 중력도, 낙하도, 착지도 없다.

### 충돌은 무한히 높은 벽이다
콜라이더는 전부 `{minX, maxX, minZ, maxZ}` — **높이 정보가 없다.**
```js
function resolve(p, axis){            // 소비처는 여기 하나뿐
  for (const c of list){
    if (p.x + R <= c.minX || ... ) continue;
    if (axis === 'x') p.x = ...       // 밀어낸다
  }
}
```
그래서 상자든 울타리든 건물이든 **똑같이 못 넘는 벽**이다.

### 콜라이더를 만드는 곳 — 9 + 1
| 위치 | 무엇 | 높이를 아나? |
|---|---|---|
| `addWall` (303) | 실내 벽 | 안다 (`WALL_S` 1.5m) |
| 건물 (459) | 야외 건물 셋 | 안다 (`bh`) |
| 정문 화분 (579) | 화분 둘 | 모델에서 잴 수 있다 |
| 울타리 (586) | 마당 사방 | 안다 (실측 가능) |
| 과일나무 (655) | 여섯 그루 | 안다 |
| 책상·소품 (775/785/799) | `PROPS` | **안다** (`p.h`) |
| 필러 (839) | 룸 사이 빈칸 | 안다 |
| `drawDecor` (906) | 꾸미기 배치 전부 | **`decorSize` 가 이미 h 를 계산한다** |

→ 높이를 새로 재야 하는 건 화분·울타리 둘뿐. 나머지는 **이미 알고 있는 값을 안 넘기고 있을 뿐**이다.

### 곡면은 GPU 에만 있다
```js
// curve.js — 버텍스 셰이더
float d = mvPosition.z - uFocus;
mvPosition.y -= d * d * uCurve;      // 뷰공간에서 아래로 만다
```
좌표·충돌·존은 평평한 그대로다. 끄는 것은 **한 줄**이다(`CURVE.value` 목표를 0 으로).
`bend()` 주입은 그대로 둬도 된다 — `uCurve = 0` 이면 아무 일도 안 한다.

---

## 2. 곡면을 없애면 잃는 것

| 잃는 것 | 크기 |
|---|---|
| 동물의 숲식 지평선(먼 땅이 말려 내려감) | **큼.** 이 룩을 만든 장치다 |
| 마을 끝이 자연스럽게 사라지는 효과 | 중간. 안개가 대신하지만 덜 부드럽다 |
| 부감 22° 에서 하늘이 보이던 이유의 일부 | 작음. 나무 띠를 낮춘 게 더 컸다 |

**얻는 것**: 보이는 높이 = 실제 높이. 점프맵의 재미는 "저 발판 위에 정확히" 인데,
곡면이 있으면 멀리 있는 발판일수록 눈과 발이 어긋난다.

> 절충안이 있다: **곡률을 0 으로 끄지 말고 낮추는 것**(0.0015 → 0.0005).
> 20m 밖에서 어긋남이 0.6m → 0.2m 로 준다. 지평선 느낌은 옅게 남는다.
> 다만 점프맵을 제대로 하려면 결국 0 이어야 한다.

---

## 3. 붙일 것

### ① 높이 있는 충돌 상자
`{minX, maxX, minZ, maxZ}` → `{..., top}` (바닥은 전부 0 이라 `top` 하나면 된다).
`decorBox` 는 `decorSize().h` 를 이미 계산하므로 한 줄 추가다.

### ② 중력과 착지
```
P.y, P.vy 추가
매 프레임:  vy -= G*dt;  y += vy*dt
groundAt(x, z) = 발밑에서 top 이 가장 높은 상자 (없으면 0)
y <= ground 면 착지 (y = ground, vy = 0)
```

### ③ resolve 가 높이를 본다
지금은 겹치면 무조건 민다. 앞으로는:
- 상자의 `top` 이 **내 발보다 낮으면** 통과(위를 걷는 중이다)
- `top - 발` 이 0.35m 이하면 **자동으로 올라선다**(계단·낮은 단)
- 그보다 높으면 지금처럼 민다

### ④ 딸려 오는 것들
- 카메라 `camLook` 이 `P.y` 를 따라간다
- 펫도 주인 높이를 따라간다(안 그러면 공중을 걷거나 땅에 묻힌다)
- 이름표·존 판정은 x/z 라 그대로
- 네트워크 좌표에 `y` 추가(남의 캐릭터도 발판 위에 서야 한다)

---

## 4. 위험한 곳

1. **꾸미기 배치가 곧 지형이 된다.** 상자를 계단처럼 쌓아 울타리를 넘어갈 수 있다.
   → 마당 밖으로 못 나가게 울타리 `top` 을 넉넉히(4m) 잡는다.
2. **바닥에 깔린 것들**(러그·타일·꽃)은 지금 `FLAT` 로 충돌에서 빼고 있다.
   높이를 붙이면 "밟고 올라서는" 대상이 되어 버린다 → `FLAT` 은 계속 제외.
3. **실내**는 벽이 허리 높이(1.15m)다. 점프로 넘어가면 방 밖으로 나간다
   → 실내 벽은 `top` 을 높게 잡거나, 실내에서는 점프 높이를 줄인다.
4. **떨어짐**. 발판에서 벗어나면 떨어진다 — 지금은 낙하 개념이 없어 아무 데도
   안 죽는다. 마당 바닥이 항상 0 이라 "떨어져 죽는" 일은 없다(구덩이가 없다).

---

## 5. 규모

- 곡면 끄기: 1 줄
- 콜라이더에 top: 10 곳, 대부분 한 줄씩
- 중력·착지·resolve 개편: 60~80 줄
- 카메라·펫·네트워크: 20 줄
- 합계 **150 줄 안팎**. 새 파일은 필요 없다.
