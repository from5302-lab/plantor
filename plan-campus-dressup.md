# 캠퍼스 캐릭터 — 조합 + 상점

작성: 2026-08-11
상태: **검토 대기 — 승인 전 코딩 없음. 결정 2개(§5) 먼저**
근거: 이 문서 §1 (12종 GLB 를 직접 뜯어 확인한 것)

## 0. 한 줄 요약
지금은 "12종 중 하나"다. 두개골이 12종 공통이라 **머리카락만 갈아 끼울 수 있고**,
리그가 같아 **몸(옷)도 통째로 바꿀 수 있다**. 캐릭터를 얼굴·머리·옷 셋으로 쪼개고,
머리와 옷을 상점에서 낱개로 판다.

## 1. 확인된 것 (에셋 실측)

- **두개골+귀가 11/12 바이트 단위로 동일**하다(해시 `6e7cd1b453bb`).
  `female-f` 만 귀가 살짝 다르다(x ±0.15~0.22, 다른 모델은 ±0.16~0.23).
  → A 의 머리카락 덩어리를 B 의 머리에 얹으면 **정확히 맞는다**.
- 머리 메시 구조가 12종 공통이다:
  `두개골 n136 · 귀 n42×2 · 눈썹 n8×2 · 입 n10` + 머리카락 덩어리 + (안경·수염 등)
- 몸 메시는 모델마다 다르다(정점 710~1052). 하지만 **뼈 7개가 이름까지 같아**
  다른 모델의 몸을 그대로 물릴 수 있다.
- **상의/하의/신발을 따로 떼는 건 안 된다.** 덩어리가 부위와 안 맞는다 —
  `male-a` 는 신발이 독립 덩어리지만 `female-c`·`male-f` 는 발·다리 살과 한 덩어리다.
  그래서 옷은 **한 벌 단위**로만 바꾼다.

## 2. 데이터

```
users/{uid}.campus.look = {
  base:   'male-a',        // 얼굴 — 두개골·눈썹·입·수염·안경이 여기서 온다
  head:   'female-c',      // 머리스타일 — 이 모델의 머리카락 덩어리만 가져온다 | 'none'
  body:   'male-f',        // 옷 한 벌 — 이 모델의 몸 메시를 쓴다
  colors: {skin, hair, top, bottom},
  aid:    'glasses' | null,
}
users/{uid}.campus.owned = { heads: ['female-c', …], bodies: ['male-f', …] }
```

**마이그레이션**: 기존 `look.model` 은 `{base, head, body}` 셋 다 그 값으로 편다.
지금 쓰던 사람은 화면이 그대로다.

## 3. 렌더 — 어떻게 합치나

1. `base` 모델을 만든다. 머리 메시에서 **머리카락 덩어리를 감춘다**
   (이미 만들어 둔 대머리 정점 목록을 그대로 쓴다).
2. `head` 모델의 머리 메시에서 **머리카락 덩어리만 남긴** 서브메시를 만들어
   `base` 의 스켈레톤에 물린다. 뼈 이름·순서가 같아 skinIndex/skinWeight 를 그대로 쓴다.
3. `body` 모델의 몸 메시로 갈아 끼운다.
4. 색은 지금처럼 UV 이사. **부위 표는 메시가 온 모델 기준**으로 찾는다
   (머리카락은 `head` 표, 몸은 `body` 표, 얼굴·살은 `base` 표).

기존 자산 재사용: 부위 표·팔레트·대머리 목록·소품 부착 모두 그대로 쓴다.

## 4. 상점

- 캐릭터 패널 안에 **탭 하나 더**: 머리 · 옷 옆에 자물쇠 표시. 안 산 것은 눌러도
  안 입혀지고 "○○P 로 사기"가 뜬다. 화면을 따로 만들지 않는다 — 입어 보는 자리와
  사는 자리가 같아야 산다.
- 안 산 것도 **미리보기는 된다**(입혀 보고 결정). 완료를 누르면 산 것만 남는다.
- 값은 `campus.bells`(잔액)에서 깎고 `owned` 에 넣는다. 이미 있는 경제다.

## 5. 결정할 것

**① 시작 캐릭터** — 기본으로 주는 것
- 얼굴: 남 `male-a` · 여 `female-a` 중 고르게 (둘 다 무료)
- 그 둘의 머리·옷은 무료. 나머지 10종의 머리·옷이 상점 물건이 된다

**② 값** — 지금 사과 하나가 몇 P 인지에 맞춰야 한다
- 제안: 머리 **300P** · 옷 **500P**. 전부 사려면 8,000P
- 지금 포인트 버는 길이 사과 판매뿐이라, **학습센터 → 포인트 연결**이 없으면
  살 수가 없다. 그게 선행되어야 상점이 의미를 갖는다

## 6. 하지 않는 것
- 상의/하의/신발 따로 고르기(§1 — 에셋이 안 갈린다)
- 얼굴(눈썹·입) 편집 · 체형 변경
- 다른 Kenney 팩 캐릭터 섞기(리그가 달라 애니메이션 공유가 깨진다)

## 7. 단계
1. `look` 을 `{base, head, body}` 로 넓히고 마이그레이션 → 검증: 기존 사용자 화면 불변
2. 머리카락 이식(`graftHair`) → 검증: 12×12 조합을 렌더해 뚫림·어긋남 확인
3. 몸 교체 → 검증: 걷기·앉기·점프에서 몸이 뼈를 따라가는지
4. 색 표를 메시 출처별로 찾기 → 검증: 조합 상태에서 부위별 색이 맞는지
5. 상점(소유·구매·자물쇠 UI) → 검증: 잔액 부족·중복 구매·새로고침 유지
6. 격리 워크트리 빌드·배포 → 라이브 확인
