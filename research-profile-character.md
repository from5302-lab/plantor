# 프로필을 캠퍼스 캐릭터 전신으로 — 리서치 (2026-08-10)

목표: 1:1 원형 프로필을 버리고, 캠퍼스에서 정한 **본인 캐릭터를 세로로 긴 전신**으로 보여준다.

## 1. 지금 프로필에 그려지는 것

`AvatarView`([src/components/learn/avatar-view.tsx](src/components/learn/avatar-view.tsx), 60줄) — 원 안에 **식물 이모지 한 글자**.
이미지 파일이 없다. 배경·테두리·이펙트는 상점 아이템의 CSS 클래스다.

| 슬롯 | 내용 | 개수·가격 |
|---|---|---|
| `base` | 식물 이모지(새싹→벚꽃…) | **수십 종 · 200~1500P** |
| `background`·`frame`·`effect` | 원형 배경·테두리·발광 | CSS |
| `cardTheme`·`xpBar`·`nameStyle` | 카드 밖 꾸미기 | CSS |

호출부 7곳: `reward-header`(64·54px) · `reward-panels`(72·34·22px) · `feed-event-card`(38·44px).
데이터는 `children/{childId}.equipped`(서버만 쓰기, 가족 읽기) + `feedEvents.equipped`(공개 비정규화).

## 2. 캠퍼스 캐릭터

`users/{uid}.campus = {look, body, room, inv, bells}` — [store.js](public/campus/lib/store.js).

**⚠ 가장 큰 제약: `users/{uid}` 는 본인만 읽는다**([firestore.rules:63](firestore.rules:63)).
- 내 `/learn` 에서 내 캐릭터를 그리는 건 된다.
- **피드·학부모 화면에서 남(자녀)의 캐릭터는 못 읽는다.** 편지함에서 겪은 것과 같은 벽이다.
→ 표시용 사본을 `children/{childId}` (가족 읽기) + `feedEvents` (공개)로 **비정규화**해야 한다.
  쓰기는 서버만이므로 캠퍼스 저장 시 콜러블을 한 번 태운다.

### 캠퍼스가 지금 갈아타는 중이다
- 기존 `avatar.js`: 절차적 생성(헤어·상의·하의 스타일 + 색 8종 + 체형 슬라이더 5개)
- 신규 `avatar-kenney.js`(HEAD): **Kenney Mini Characters 12종 중 하나를 고르는 방식**.
  803삼각형 · 7뼈 · 클립 공유 · 팔레트 텍스처 1장(NearestFilter 필수) · GLB 90~200KB.
  주석에 "꾸미기는 '어느 캐릭터를 고를까'로 바뀐다" 고 적혀 있다.
→ `look` 스키마가 아직 확정이 아니다. **프로필은 look 을 해석하지 말고 통째로 넘겨** 캠퍼스 렌더러가 그리게 해야 한다.

## 3. 어떻게 그릴까 — 세 갈래

| 방식 | 비용 | 판정 |
|---|---|---|
| 카드마다 실시간 three.js | 피드 20장 = WebGL 컨텍스트 20개. 모바일에서 죽는다 | ✗ |
| **저장 시 PNG 스냅샷** | 캠퍼스에서 캔버스로 세로 컷 → Storage → `<img>` 한 장 | **피드·목록에 이것** |
| **실시간 3D 뷰어 1개** | 프로필 화면에만. three.js 는 `dynamic(ssr:false)` 로 분리 | **프로필에 이것** |

- three.js 는 지금 `/campus`(정적 HTML, CDN import)에만 있다. Next 번들에 그냥 넣으면 안 된다(~250KB gzip).
- Storage 규칙에 **공개 읽기 경로가 없다** — `screenshots/**` 는 로그인 필요. 피드는 비로그인도 읽으므로
  `serviceIcons/**`처럼 `allow read: if true` 인 경로(`campus-shots/{childId}/…`)가 새로 필요하다.

## 4. 비율

Kenney 치비는 2등신이라 전신 실루엣이 대략 1 : 1.3~1.4다.
- **3:4** — 전신이 프레임을 꽉 채운다. 권장.
- 2:3·1:2 — 위아래 여백이 남아 캐릭터가 작아 보인다. "길게"의 대가로 캐릭터가 작아진다.

## 5. 부딪히는 결정 — 식물 상점

프로필의 주인공이 캐릭터가 되면 **`base` 슬롯(식물 이모지)의 자리가 없어진다.**
학생들이 200~1500P 주고 산 것들이다. 세 갈래:

| 안 | 내용 | 대가 |
|---|---|---|
| **(a) 발치의 화분으로 살린다** | 캐릭터 옆/발치에 산 식물을 놓는다 | 산 게 살아남고, 플랜토의 '식물·성장' 정체성과도 맞는다 |
| (b) 캐릭터 파츠 상점으로 교체 | 식물 아이템 폐기·환불 | 보상 신뢰가 깨진다 |
| (c) 큰 곳만 캐릭터, 작은 곳은 식물 | 정체성이 둘로 갈린다 | 같은 사람이 두 얼굴 |

## 6. 지금 할 수 있는 것 / 캠퍼스 확정 후

- **지금**: 프레임 규격(1:1→3:4)·레이아웃 전환, 표시용 사본 경로(콜러블·필드·규칙·Storage),
  스냅샷 파이프라인 뼈대, 식물 상점 정책 결정
- **캠퍼스 확정 후**: 실제 `look` 스키마 반영, 뷰어 카메라·포즈(idle), 파츠 상점 재매핑
