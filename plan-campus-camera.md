# 캠퍼스 카메라 기본각 — plan

작성: 2026-08-11
상태: **완료** — 33° 로 승인·구현·배포 후 28° 로 한 단계 더 내렸다(2026-08-11)
근거: `research-campus-camera.md`

## 0. 한 줄 요약
동숲과 갈리는 건 부감이 아니라 **수평각**이다. 기본 45°(대각선)를 **0°(정면 정렬)**
로 내리고, 부감을 낮춰 지평선을 보이게 한다. 회전·줌 기능은 그대로 둔다.

## 1. 바꾸는 것

**① 기본 수평각 45° → 0°** — [map.js:998](public/campus/lib/map.js:998)
```js
let camYaw = 0, camYawTo = 0;
```
J/K 회전은 남는다. 45° 단위도 그대로 — 0°에서 시작하면 0·45·90… 으로 돌아
정면과 대각선을 둘 다 쓸 수 있다(지금은 45·90… 이라 정면이 아예 안 나온다).

**② 야외 스폰 방향 `-3π/4` → `π`** — [map.js:157](public/campus/lib/map.js:157)
뒷모습으로 시작한다는 기존 의도를 새 기본각에 맞춘 것이다. 주석의 45° 설명도 고친다.

**③ 야외 부감 40.6° → 낮춘다** — [map.js:157](public/campus/lib/map.js:157)
거리(27.7m)는 유지해 피사체 크기를 그대로 두고 각도만 내린다. §2 에서 고른다.

## 2. 결정할 것 — 야외 부감을 얼마나 내릴까

| 안 | 부감 | `camR` | `camH` | 결과 |
|---|---|---|---|---|
| A | **33°** | 23.2 | 15.1 | 지평선이 살짝 보인다. 코드에 남아 있던 옛 기본값이라 한 번 검증된 각도 |
| B | **28°** | 24.4 | 13.0 | 하늘이 확실히 열리고 곡률이 눈에 띈다. 동숲에 제일 가깝지만 건물 뒤가 더 가려진다 |
| C | 40.6° 유지 | 21.0 | 18.0 | 수평각만 바꾼다. 변화 폭이 가장 작다 |

권장은 **A**. 한 번에 둘(수평각·부감)을 다 바꾸는 만큼, 부감은 검증된 각도로
한 단계만 내리고 라이브 스크린샷을 보고 B 로 더 갈지 판단하는 게 안전하다.

## 3. 하지 않는 것 (이번 범위 밖)

- **실내 부감**(34.5°) — 실내는 곡률이 0 이고 허리벽이 있어 각을 내리면 벽이 시야를
  먹는다([map.js:997](public/campus/lib/map.js:997) 주석). 야외부터 보고 판단한다.
- **화각(FOV 30°)** — 더 줄이면 더 납작해지지만 거리를 35m 쯤으로 늘려야 하고,
  그러면 안개(46–105)와 곡률 상수를 같이 다시 맞춰야 한다. 별건으로 남긴다.
- **ACNH 식 3단계 틸트 전환** — 지금은 기본값 문제지 조작 문제가 아니다.
- 회전·줌 기능 제거.

## 4. 단계

1. `camYaw` 기본값 0 · 야외 `spawn.yaw` π · 주석 갱신 → 검증: 타입 무관, `eslint`
2. 야외 `camR`/`camH` 를 선택안으로 교체 → 검증: `npm run build` 성공
3. 격리 워크트리에서 빌드·배포(다른 세션 미커밋 변경이 섞여 있다)
4. 라이브 스크린샷으로 확인 → 검증 항목:
   - 건물 셋이 정면으로 서고 벤치·울타리가 화면 축과 나란한가
   - 캐릭터가 뒷모습으로 시작하는가
   - 지평선이 흰 배경으로 자연스럽게 녹는가(바닥 판 끝이 안 보이는가)
   - 학습센터·우리집·상점에 들어갔을 때 캐릭터가 등 돌린 채 어색하지 않은가
     (실내 스폰 방향은 45° 기준으로 잡혀 있다 — 어긋나면 여기서 조정)
   - J/K 회전과 줌이 그대로 동작하는가
