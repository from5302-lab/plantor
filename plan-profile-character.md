# 프로필 = 캠퍼스 캐릭터 전신(세로) — plan

작성: 2026-08-10
상태: **검토 대기 — 승인 전 코딩 없음. 결정 2개(§1) 먼저**
근거: `research-profile-character.md`

## 0. 한 줄 요약
캠퍼스에서 정한 캐릭터가 곧 그 사람이다. 프로필은 원형 1:1 이모지를 버리고
**3:4 세로 프레임의 전신 캐릭터**가 된다. 큰 자리는 실시간 3D, 작은 자리는 스냅샷 PNG.

## 1. 확정된 결정 (2026-08-10 사용자)

**① 식물 상점(`base`)은 삭제한다.** 화분으로 살리는 안은 기각.
- 프로필의 주인공은 캐릭터 하나다. 식물을 옆에 붙이면 주인공이 둘이 된다.
- **다만 순서가 있다**: 캐릭터가 실제로 뜨기 전에 지우면 프로필에 아무것도 안 남는다.
  → 삭제는 §6 2단계에서, 캐릭터 렌더와 **같은 배포**로 나간다.
- 산 학생에게는 **쓴 포인트를 자동 환불**한다(`children/{id}.pointsSpent` 기록이 있다).
  보상으로 준 것을 말없이 회수하면 그 다음 보상도 안 믿는다.
- 포인트 쓸 곳이 `base` 하나뿐이었다 → 삭제 후 소비처는 캠퍼스 쪽(가구·아이템)으로 넘긴다. 별건.

**② 세로로 바꾸는 범위는 프로필 카드만.**
- 바꾼다: `/learn` 최상단 카드, 피드 맨 위 프로필 카드, 학부모 자녀 카드 (`RewardHeader showIdentity`)
- 그대로 둔다: 피드 이벤트 카드의 38·44px, 상점 목록의 22·34px — 전신이 뭉개진다.
  이 자리는 **얼굴만 크롭한 원형 썸네일**로 바꿔 정체성만 잇는다.

**③ 카드 높이는 늘리지 않는다.** 96px 유지 — 아래 §4.

## 2. 데이터 — 표시용 사본을 만든다

`users/{uid}.campus` 는 본인만 읽는다. 남에게 보이려면 사본이 필요하다.

```
children/{childId}
  campusLook: { …look 원본 그대로… }   ← 해석하지 않고 통째로 보관(스키마 미확정)
  campusBody: { … }
  campusShot:     "https://…/campus-shots/{childId}/full-{v}.png"   전신 3:4
  campusFaceShot: "https://…/campus-shots/{childId}/face-{v}.png"   얼굴 1:1
  campusLookAt:   Timestamp
feedEvents/{id}
  campusFaceShot                        ← 피드 카드용(공개). 없으면 지금의 이모지 아바타로 폴백
```

- **콜러블 `publishCampusLook({ look, body, fullPng, facePng })`** — 캠퍼스 저장 시 호출.
  호출자를 `resolveChild()` 로 본인 학생에 묶고, Storage 업로드는 서버가 한다(경로 위조 방지).
  하루 호출 상한을 둔다(꾸미기를 계속 만지면 업로드가 계속 일어난다).
- Storage 규칙 신설: `campus-shots/{childId}/{file}` — `allow read: if true`, 쓰기는 서버만.
  피드는 비로그인도 읽으므로 공개 경로여야 한다.
- 캠퍼스는 게스트도 쓴다 → 로그인 학생일 때만 발행한다(게스트는 지금처럼 로컬 저장).

## 3. 렌더

**A. 프로필 카드 — 실시간 3D `<CharacterView>`**
- `next/dynamic(ssr:false)` 로 three.js 를 프로필에서만 불러온다. 정적 export 에는
  `dynamic` 한 줄로 붙는다(research.md §2 에서 확인된 방식).
- 캠퍼스 렌더러를 **재사용**한다: `public/campus/lib/avatar-*.js` 를 그대로 import 해
  `buildAvatar(look, body)` → 카메라 정면·전신, `poseAvatar(rig,'idle')` 만 돌린다.
  프로필용 렌더러를 새로 쓰면 캠퍼스와 다른 얼굴이 되고, 캠퍼스가 바뀔 때마다 두 번 고쳐야 한다.
- 캔버스는 3:4, `devicePixelRatio` 상한 2, 탭이 안 보이면 렌더 정지.
- 실패·미설정: 캠퍼스에 캐릭터가 없으면 **"캠퍼스에서 내 캐릭터 만들기" 안내 카드**를 세로 프레임에 넣는다.

**B. 남의 카드(학부모가 보는 자녀·피드 상단) — `campusShot` PNG**
3D 뷰어는 내 카드에서만. 남의 캐릭터는 사본 PNG 로 충분하고, 권한도 그게 맞다.

**C. 작은 자리 — `campusFaceShot` 원형 썸네일**
`AvatarView` 에 `shot?: string` 을 받아, 있으면 이미지를, 없으면 지금의 이모지를 그린다.
기존 7개 호출부는 손대지 않는다.

## 4. 레이아웃 — 카드는 그대로, 캐릭터가 카드 밖으로 선다

높이를 늘리지 않기로 했으므로 **캐릭터를 카드 안에 가두지 않는다.** 게임 프로필의
스탠디(입간판)처럼, 머리는 카드 위로 조금 솟고 발은 카드 아래로 조금 내려선다.

```
        ╭─╮                        ← 머리가 카드 위로 14px (페이지 상단 여백 24px 안에서)
┌───────┼─┼────────────────────┐
│      ╭┴─┴╮   정승원      중2  │
│      │   │   🎖️🌱 Lv.12 🔥5일  │   카드 높이 96px — 지금 그대로
│      ╰┬─┬╯   ▓▓▓▓▓░░░ 280/500 │
│       │ │    [🎖️ 뱃지] [⭐ 상점]│
└───────┼─┼────────────────────┘
        ╰─╯                        ← 발이 카드 아래로 10px (카드 아래 여백 20px 안에서)
```

- 캐릭터 프레임 **90×120 (3:4)**, `margin-top:-14px; margin-bottom:-10px`.
  카드의 콘텐츠 높이는 정보 칼럼(약 96px)이 계속 결정한다 → **페이지 흐름 높이 변화 0.**
- 잘리지 않는 근거: `html, body { overflow-x: clip }`([globals.css:7](src/app/globals.css:7)) —
  `hidden` 이 아니라 `clip` 이라 세로 넘침은 살아 있다. 카드에도 `overflow:hidden` 이 없다
  (이펙트가 원 밖으로 번져야 해서 원래부터 안 걸어 뒀다).
- 빌릴 여백이 실제로 있다: 페이지 상단 `pt-6`(모바일 24px)·`pt-12`(데스크톱 48px),
  카드 아래 `mb-5`(20px). 위 14 / 아래 10 은 그 안에 들어간다.
- 캐릭터를 왼쪽에 두고 정보는 오른쪽 세로 쌓기. 모바일 343px 기준 정보 영역 220px 확보.
- 위험: 스탠디가 상단바에 8px까지 접근한다. 상단바는 sticky 라 스크롤 시 겹칠 수 있으므로
  캐릭터 `z-index` 는 상단바보다 낮게 둔다(겹치면 상단바가 이긴다).

## 5. 하지 않는 것
- 피드 이벤트 카드마다 3D 렌더 · 캐릭터 애니메이션(걷기·춤) · 배경 씬 렌더
- 캠퍼스 룩 스키마 해석(파츠별 UI) — 캠퍼스가 확정될 때까지 통째로 보관만
- 기존 상점 아이템 폐기·환불

## 6. 단계

**1단계 — 캠퍼스 확정 전에 할 수 있는 것**
- [ ] `publishCampusLook` 콜러블 + `children` 표시용 필드 + Storage 공개 경로·규칙
- [ ] 규칙 테스트(남의 childId 로 발행 차단, 공개 읽기 확인)
- [ ] `AvatarView` 에 `shot` 폴백 추가(그림이 없으면 지금 그대로)

**2단계 — 캠퍼스 캐릭터 확정 후 (한 배포로 묶는다)**
- [ ] 캠퍼스 저장 시 3:4 전신 + 1:1 얼굴 캔버스 캡처 → 발행 (`public/campus` 는 지금 다른 작업 중이라 대기)
- [ ] `<CharacterView>` (캠퍼스 렌더러 재사용, dynamic import)
- [ ] `RewardHeader showIdentity` 스탠디 레이아웃 전환
- [ ] **식물 상점 삭제** — `SHOP_ITEMS` 의 `base` 슬롯 제거 · `DEFAULT_ITEMS` 정리 ·
      `AvatarView` 의 이모지 경로 제거 · 구매 이력 있는 학생에게 포인트 환불(1회성 스크립트) ·
      `feedEvents.equipped.base` 는 읽지 않게. **캐릭터 렌더와 같은 배포로만 나간다**

**검증**: 타입체크·lint·정적 빌드 · 규칙 테스트 · 라이브에서 본인/자녀/비로그인 3인칭 확인 ·
번들 크기 확인(three.js 가 프로필 밖 페이지에 새어들면 실패로 본다)
